# Аудит документации и стека — 2026-08-04

Сплошная сверка всей живой документации с работающим стеком: что соответствует реальности,
что молча врёт, что является замыслом, и какой функционал заявлен, но не доведён.

- Реестр недоделок: **[TODO-UNFINISHED.md](./TODO-UNFINISHED.md)** — 52 пункта
- Архив отложенного: **[archive/README.md](./archive/README.md)**

---

## 1. Как это проверялось

Сверка шла в три прохода, все утверждения — против живого сервера, а не против документов.

1. **Снимок реальности.** Пять независимых зондов сняли фактическое состояние: работающий стек
   (compose, образы, порты, cron, Prometheus), карта кода (что подключено к рантайму, что осиротело),
   данные (PostgreSQL/Qdrant/Neo4j/Redis, воркспейсы, источники), состояние веток и деплоя,
   инвентаризация маркеров недоделок.
2. **Сверка документов.** Десять групп документации прочитаны и сверены с этим снимком и с кодом.
   Каждое фактическое утверждение — порт, имя сервиса, схема таблицы, команда — проверялось отдельно.
3. **Скептическая перепроверка.** Каждый вердикт «устарел / в архив» отдавался второму проверяющему
   с заданием **опровергнуть** его.

Разобрано **79 документов** (71 живой `.md` + `.cursor/rules/*.mdc` + корневые инструкции),
без архива предыдущего проекта и книжных саммари.

### Чего эта проверка стоит — честно

Третий проход был не формальностью: **11 вердиктов из 79 отменены**, и среди них — **все четыре
предложения «отправить в архив»**. Типовая ошибка первого прохода: датированный снимок или
намеренный план принимался за документ, который «врёт про сейчас».

Две ошибки я поймал уже собственной проверкой, после того как зонды разошлись между собой:

| Расхождение | Как на самом деле |
|---|---|
| Один зонд сообщил, что коллекция `own_corpus__embeddingsgigar__dense_2560` наполнена (4647 точек) | Коллекции **нет**. `GET /collections` отдаёт четыре: `frontier_docs`, `trend_clusters` и две версионированные. Прав был второй зонд |
| Память проекта утверждала, что 26 веток не запушены | Все ветки **запушены**, каждая `SYNCED` с `origin/<той же>`. Риска потери кода нет; проблема в том, что они не влиты |

Вывод для чтения этого отчёта: числа проверяемы, но не безошибочны. Каждый пункт снабжён
командой или ссылкой `файл:строка` — при сомнении перепроверяйте, а не верьте на слово.

---

## 2. Стек: что работает на самом деле

Снято 2026-08-04, ~05:35 UTC.

**Все 18 объявленных сервисов запущены.** Ни одного упавшего, ни одного лишнего.
RestartCount = 0 у 17 из 18; исключение — `crawl4ai` с 84 рестартами.

| Слой | Состояние |
|---|---|
| Данные | PostgreSQL 23 таблицы, 2249 MB; posts 311 269, post_enrichments 660 741 |
| Qdrant | 4 коллекции, рабочие 2 через алиасы: `frontier_docs_active` (190 087 точек), `trend_clusters_active` (374) |
| Neo4j | 948 429 узлов, 11 334 112 связей. Concept 758 337, Document 190 085 |
| Redis | 255 MB из 2 GB, все 4 consumer-группы с `pending=0` и `lag=0` |
| Источники | 247 в БД: 202 включены, 45 выключены |
| Воркспейсы | 6, все получают свежие данные в день аудита |
| Мониторинг | 8 таргетов, все `up`; 56 правил алертов; firing — ноль |

### Что в стеке устроено не так, как написано

**Профилей `rag` и `analytics` не существует.** Реальных десять: `core, ingest, xray, worker,
crawl, paddleocr, mcp, admin, searxng, monitor`. Все 18 сервисов объявлены с `profiles:`,
а `COMPOSE_PROFILES` в серверном `.env` не задан — поэтому голый `docker compose up -d`
поднимает **ноль** сервисов, а `docker compose logs mcp` падает с обманчивым
`no such service: gpt2giga-proxy`. Команды из `CLAUDE.md` и `AGENTS.md` не работали.

**Три из четырёх точек входа деплоя собраны с невалидным набором профилей** и падают на
`docker compose config`: `scripts/server-deploy-rebuild.sh`, `Makefile ALL_PROFILES` и рецепт
`--profile monitor` из `docs/monitoring-runtime-dashboard.md`. Причина одна: `ingest`, `admin`
и `crawl4ai` объявляют `depends_on: xray`, а профиль `xray` в наборы не включён. Рабочий эталон
один — `scripts/server-build-stack.sh`.

**Дрейф кода между диском и образами.** Проверено по checksum, не по датам файлов:
`ingest` и `mcp-gateway` работают на устаревшем `shared/config.py` (в образе нет полей
`own_stake_*`, `searxng_empty_cache_ttl`; у `mcp-gateway` ещё и старые модели polza).
У `worker`, `mcp` и `admin` дрейфа нет.

> Сравнение по mtime здесь вводит в заблуждение: у `crawl4ai` и `paddleocr` файлы на диске
> «новее образа», но содержимое совпадает — это следы массового rsync. Судить можно только
> по checksum.

**LangChain и gigachain в проекте отсутствуют.** Ни один сервис их не ставит; `worker` работает
на `openai==1.54.4`, а `worker/gigachat_client.py:1` прямо документирует отказ от LangChain.
Маршрутизация — собственный control plane поверх четырёх провайдеров с горячей перезагрузкой
из `admin_runtime_settings.llm_routing_v1`. GigaChat обслуживает эмбеддинги и `mcp_synthesis`,
но не основную цепочку обогащения — она ушла на wormsoft.

---

## 3. Документация: вердикты

| Статус | Сколько | Что означает |
|---|---|---|
| ⚠️ **STALE** | 7 | Подан как описание «сейчас», но расходится с рабочим стеком |
| 🟡 **PARTIAL** | 28 | Основа верна, часть утверждений разошлась |
| 📐 **ASPIRATIONAL** | 30 | Замысел/план, не реализован. Не устарел — просто не начат |
| 📌 **HISTORICAL** | 11 | Датированный снимок или решение своего момента |
| ✅ **ACTUAL** | 3 | Соответствует реальности |

Три документа прошли сверку без замечаний: `docs/provenance-independence-layer.md`,
`docs/saas/10-decision-log.md`, `docs/audit/llm-orchestrator-weekly-snapshot.md`
(последний — намеренный бланк, и пустые поля для него норма).

### Устарело всерьёз

**`docs/README.md` — 35 расхождений.** ✅ *Закрыто заходом 4 в тот же день; ниже — состояние
на момент аудита, ради истории.* Главная спецификация проекта разошлась с системой
по всем измеряемым осям:

- Схемы таблиц описаны неверно: `post_enrichment` вместо `post_enrichments` и с другим составом
  колонок; в `sources` фигурируют `type`/`enabled`/`schedule`/`categories`, а в базе
  `source_type`/`is_enabled`/`schedule_cron` и никаких `categories`; таблиц `post_media_map`
  и `media_group_items` не существует вовсе.
- Коллекции Qdrant названы базовыми именами — они мертвы; рабочие доступны через алиасы.
- Метки Neo4j `(:Source)`, `(:TrendCluster)`, `(:Album)` и связи `EVOLVED_FROM`, `FROM_SOURCE`,
  `BRIDGES` в графе отсутствуют. Реально живут `MENTIONS`, `RELATED_TO`, `CONTAINS` и `ECHO_OF`,
  причём последняя в README не описана.
- Порядок xray-профилей описан неверно: все три перечисленных **выключены**, работает не
  упомянутый в документе `profile_e_tls_tm_ws`.
- `INDEXING_MAX_CONCURRENCY=1` — на сервере 4.
- Блок «XRAY Runtime Operations» неисполним: все `/api/monitoring/*` закрыты авторизацией и
  отвечают 401.

**`.cursor/rules/` — пять файлов из восьми устарели** (`01-project-context`, `02-tech-stack`,
`03-pipeline`, `04-database`, `05-mcp-api`), от 8 до 11 расхождений в каждом.

**`docs/problematic-sources.md`** — недатированный снимок трёхмесячной давности: заявлено
7 выключенных источников, в базе 45. Единственный источник, названный выключенным по high fail-rate,
сейчас включён и успешно отрабатывает. Уцелела только секция «повторная проверка на сервере» —
приведённая команда выполняется как есть.

### Корневые инструкции агентов

`CLAUDE.md` (14 расхождений) и `AGENTS.md` (11, те же самые) — самый дорогой класс ошибок:
они загружаются в каждую сессию и учат неверному. Помимо профилей `rag`/`analytics`:

| Написано | На самом деле |
|---|---|
| LangChain / gigachain — основной стек | Их нет в проекте; `openai` SDK напрямую |
| Qdrant, per-workspace коллекции | Одна коллекция на модель, изоляция payload-фильтром |
| 3 воркспейса | 6 |
| `structlog` — не `print()` | Основной код (59 файлов) на stdlib `logging`; structlog только в `crawl4ai/` и `services/paddleocr/` |
| `select(Model)`, SQLAlchemy 2.x | `sqlalchemy.select` не импортируется нигде; всюду сырой SQL через `text()` |
| Секреты через `os.environ["KEY"]` | `os.environ[...]` не используется в прод-коде ни разу; конфигурация на `pydantic-settings` |
| Маркеры тестов `unit/integration/e2e` | `e2e` не зарегистрирован в `pyproject.toml` |
| Не использовать Telegram Bot | Используется в проде для алертов |
| Rate limiting: Redis leaky bucket | Входящего rate limiting нет вовсе |

**Оба файла исправлены по существу.** `AGENTS.md` теперь пересобирается из `CLAUDE.md`:
до аудита это был его дословный форк, файлы уже разошлись, и каждая правка требовала двойного
применения.

---

## 4. Что сделано с документами

### Помечено (74 файла)

В шапку каждого документа, разошедшегося с реальностью, вставлена датированная пометка
с маркером `<!-- audit-status:2026-08-04 -->`. Четыре вида: ⚠️ устарело, 🟡 частично устарело,
📌 исторический снимок, 📐 замысел не реализован. Документ **без** пометки — сверен и актуален.

Перекрытые версии в `docs/harness/` получили дополнительную строку «Перекрыт: **vN**» —
именно её отсутствие аудит отмечал как главный дефект этой цепочки.

### Отложено (1 файл)

`docs/deep-research-report (6).md` → `docs/archive/2026-03_deep-research-signal-aggregation.md`.
Внешний доимплементационный ресёрч, написанный до проекта; систему не описывает, ссылок на него
в репозитории нет, имя файла — мусор от скачивания браузером. Заодно переименован.

### Что сознательно НЕ отложено

**Перекрытые версии harness/saas остались на месте.** Владелец зафиксировал это решение
коммитом `f8d7bbf` (03.08.2026): «v2 и v3 спорят с предыдущими версиями и короче их;
удалить v1 — оставить поздние документы спорящими с пустотой. *The delta IS the content —
this is a decision record, not a pile of drafts*». Архивирование прямо противоречило бы
свежему решению, поэтому — пометка вместо переноса.

**Устаревшие, но несущие документы** (`docs/README.md`, `.cursor/rules/*`, раннбуки) остаются
в обращении: они единственное место, где записаны рабочие процедуры. Им нужна правка, а не архив.

---

## 5. Незаконченный функционал — коротко

Полный реестр с доказательствами: **[TODO-UNFINISHED.md](./TODO-UNFINISHED.md)** (52 пункта).

Пять блокеров:

1. **Кластеризация видит 400 постов на воркспейс в сутки** — трендовый слой питается ~29% корпуса
   (55 308 из 187 944 подходящих постов). Потолок зашит в `semantic_cluster_max_posts=400`,
   крон — раз в сутки, backfill-джобы нет.
2. **RSI-контур одобрения недоступен.** Десять инструментов работают по REST, но ни один не выведен
   в MCP-шлюз — единственный клиент их не видит. Следствие: 86 предложений слияния сущностей висят
   `pending` месяц, у всех 26 911 `relevance_decisions` вердикт `NULL`.
3. **MCP-шлюз 8102 без аутентификации** — и на нём уже есть пишущие инструменты
   (`record_card_feedback`, `ingest_url`). Соседний REST-порт 8100 закрыт на loopback именно
   по этой причине; шлюз остался снаружи.
4. **Весь алертинг ходит через `admin`**: 135 из 296 доставок за 7 дней провалились (46%),
   и алерт о падении самого `admin` доставить физически нечем. Резервного пути и watchdog нет.
5. **Бэкапы делаются, восстановление не написано и ни разу не проверено.** Ни одного скрипта
   с `restore` в имени, ни раннбука, ни верификации дампа, ни учения.

Самый частый класс проблем — **молчаливый отказ**: 41% задач `crawl4ai` теряется без записи
и метрики; 42% vision-обогащений делаются для постов, которые тут же выбрасываются;
`stream:posts:enriched` пишется без единого подписчика; DLQ объявлен, но никем не читается;
RSI-метрики зарегистрированы, но не отдают ни одной серии, из-за чего два алерта не сработают
никогда.

---

## 6. Синхронизация локально ↔ сервер

**Код совпадает** — rsync dry-run в обе стороны не показывает расхождений по содержимому.
Единственный серверный каталог, которого нет локально, — `docs/ops/alert-digests/`
(намеренно исключён, там копится история дайджестов).

### Починен путь «Sync ← Server»

`scripts/sync-pull.ps1` **не запускался вообще** — две независимые поломки:

1. Висячая запятая перед комментарием в массиве аргументов → `ParserError: Missing expression after ','`.
2. Файл в UTF-8 **без BOM**. PowerShell 5.1 читает такой файл как ANSI, байт `0x94` из кириллицы
   и типографики превращается в закрывающую «умную кавычку» `”`, которую парсер принимает за
   конец строки — и `Write-Host` рвётся посреди сообщения.

Обе исправлены, скрипт проверен парсером и отработал dry-run.
Задача Cursor «Sync ← Server» ссылалась ровно на этот файл.

> Остальные восемь `.ps1` в `scripts/` сейчас парсятся, но лежат без BOM и содержат кириллицу —
> это та же мина с невзведённым бойком. Пункт заведён в реестр.

### Одновременная работа в том же дереве

Во время аудита **в этом же рабочем дереве работала другая сессия**. Установлено:

- в 09:11 локально изменён `scripts/init_storage.py`, затем синхронизирован на сервер;
- в 09:12:50 UTC+3 на сервере создана ветка `chore/drop-own-corpus-from-init-storage`,
  сделан коммит «storage: stop creating the author-corpus collection at init» и запушен в origin;
- HEAD сервера переставлен с `test/searxng-ttl-field-contract` на новую ветку.

Проверено, что это **не** работа агентов аудита: сканирование всех 36 транскриптов дало
**ноль** записей в репозиторий, все совпадения по «пишущим» шаблонам оказались read-only-зондами
(`points/scroll`, `points/count`, `tools/list`, health-проверки).

Практическое следствие: `sync-push` с `--delete` в такой момент может затереть чужую
незасинхронизированную работу. **Отправка на сервер отложена до явного решения.**

### Ветки

27 веток не влиты в `main`, уложены стопкой (каждая +1 коммит к предыдущей). `main` заморожен
на 2026-07-03. Все ветки запушены в origin, поэтому риска потери кода нет — но `origin/main`
как источник правды показывает состояние месячной давности, а `git checkout main` на рабочей
копии сервера откатил бы месяц исправлений.

Отдельно: в рабочем дереве сервера лежит **незакоммиченная** правка
`admin/backend/routers/pipeline.py` — ручной reprocess пишет в стрим с явным потолком
`maxlen=STREAM_MAXLEN`. Это защита ровно от того Redis-OOM, что положил стек 31.07 на 9 часов,
и она уже раскатана в работающий образ. Любая пересборка из git HEAD молча её снимет.

---

## 7. Маршрут

Порядок утверждён владельцем 2026-08-04. Работа рассчитана на несколько подходов в разных
сессиях, поэтому каждый заход самодостаточен: цель, что делать, и **чем проверяется, что
получилось**. Отмечайте галочки — по ним видно, где остановились.

### Заход 0 — твёрдая земля (предусловие всего остального) — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Закоммитить `admin/backend/routers/pipeline.py` на сервере
- [x] Прогнать тесты
- [x] Fast-forward `main` на верхушку стопки и пуш

**Как прошло.** Три коммита поверх стопки: `fix/admin-reprocess-stream-cap` (защита
`maxlen=STREAM_MAXLEN` плюс `rf`-строки взамен невалидного `\s` в f-строках),
`fix/sync-pull-ps1-parses`, `docs/audit-2026-08-04`. `main` перемотан
`595a977` → `303575a`, 30 коммитов, ноль merge-коммитов, запушен вместе со всеми ветками.

Перемотка сделана `git fetch . <ветка>:main`, **без `git checkout main`**. Это важно:
переключение дерева на июльское состояние — пусть на секунду — подсунуло бы prometheus
и searxng их старые bind-mount конфиги.

Тесты: **908 passed, 5 skipped, 0 failed**; ruff (`E9,F63,F7,F82,F811`) — `All checks passed`.

> **Грабля, на которой легко успокоиться раньше времени.** Прогон в admin-образе даёт
> 10 ошибок сборки на отсутствии `bs4` — они задокументированы как environmental, и
> соблазн списать их велик. Но CI ставит зависимости из `requirements-dev.txt`, где есть
> и `beautifulsoup4`, и `lxml`, то есть **в CI эти тесты реально исполняются**. Проверять
> надо тем же набором, что у CI: `pip install -r requirements-dev.txt`, иначе десять тестов
> остаются непроверенными, а выглядит это как «известная норма».

Не «шаг», а двухминутная страховка. Правка `pipeline.py` — это `maxlen=STREAM_MAXLEN`
в ручном reprocess, защита от Redis-OOM 31.07; она уже в работающем образе, но **любая
пересборка из git HEAD молча её снимет**. Это единственный пункт во всём отчёте, который
активно ухудшается со временем.

Слияние оказалось дешёвым: стопка строго линейна — 27 коммитов, **ноль merge-коммитов**,
`git merge-base --is-ancestor main HEAD` → да. То есть fast-forward, а не разбор конфликтов.

Побочный эффект, ради которого стоит сделать это первым: в `.github/workflows/ci.yml` есть
рабочий гейт (ruff на реальные баги + unit-тесты), но триггеры у него `push: branches: [main]`
и `pull_request`. `main` не двигался с 2026-07-03, PR-процесса нет — **значит CI не проверил
ни одного из 27 коммитов**. Слияние впервые за месяц включит проверку.

**Готово, когда:** `git status` на сервере чист по `pipeline.py`, `main == HEAD`, CI зелёный.

---

### Заход 1 — резервный путь алертинга — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Прямой `telegram_configs` в Alertmanager мимо `admin`
- [x] Watchdog-правило (dead-man's switch): алерт, который всегда firing, и внешняя проверка его тишины
- [x] Токен вебхука из query string в Basic-auth
- [x] Fail-closed при пустом токене (образ `admin` пересобран и раскатан)
- [x] Живая проверка доставки обоими путями

**Что сделано.** Receiver `telegram-direct` (`prometheus/alertmanager.yml`) ходит в
Telegram сам, минуя `admin`. Одиннадцать критических правил теперь идут **обоими**
путями: маршрут с `continue: true` плюс явный catch-all. Проверено
`amtool config routes test`:

| Алерт | Куда уходит |
|---|---|
| `severity=critical` | `telegram-direct,telegram-admin` |
| `severity=warning` | `telegram-admin` |
| `alertname=FrontierWatchdog` | `blackhole` |
| `notify=never` | `blackhole` |

Dead man's switch: правило `FrontierWatchdog` (`expr: vector(1)`, firing всегда,
глушится в blackhole) плюс внешний наблюдатель `scripts/alert-watchdog.sh` в cron
каждые 10 минут. Он опрашивает Prometheus и Alertmanager по loopback, проверяет
присутствие watchdog-алерта и рост `alertmanager_notifications_failed_total`, и при
поломке шлёт сообщение в обход и `admin`, и маршрутизации Alertmanager. За ним самим
следит правило `FrontierWatchdogStale`.

**Три вещи, которые оказались не такими, как ожидалось.**

*Прямой путь не бывает прямым.* Egress к `api.telegram.org` с хоста закрыт наглухо
(`Network is unreachable`), работает только socks5 `xray:10808` — и он резолвится
лишь внутри docker-сети. Поэтому «мимо admin» означает «своим маршрутом через тот же
прокси», а наблюдатель на хосте отправляет сообщение через `docker exec` в первый
живой контейнер (`admin` пробуется последним — он подозреваемый).

*`chat_id` у Alertmanager — это int64.* В `.env` лежит `@имя` канала: Alertmanager
отвечает `cannot unmarshal !!str into int64`, а YAML вдобавок не разрешает голому
скаляру начинаться с `@`. Заведена отдельная `ALERT_TELEGRAM_CHAT_NUMERIC_ID`;
`@имя` осталось для Bot API в `admin`, где оно допустимо.

*URL прокси не подставляется через `sed`.* В нём учётные данные, а `&` в пароле
sed трактует как «весь совпавший текст» и молча портит замену. Прокси уехал в
`HTTPS_PROXY` контейнера и читается через `proxy_from_environment: true` — флаг
действует только на `telegram-direct`, вебхук в `admin` по-прежнему идёт напрямую
по внутренней сети.

> **Грабля, стоившая получаса ложной тревоги.** Проверяя авторизацию вебхука,
> я получил 403 на все способы, включая рабочий, и чуть не записал это как поломку.
> Причина была в проверке: `docker exec` **без `-i`** не пробрасывает stdin, поэтому
> токен приходил в контейнер пустым. В логах `admin` это видно дословно —
> `?token=` без значения. Настоящий клиент всё это время получал 200.

**Проверено вживую.** Синтетический критический алерт через `amtool alert add` ушёл
в оба receiver'а, счётчики Alertmanager до и после:

| Интеграция | До | После | Провалов |
|---|---|---|---|
| `telegram` (прямой путь) | 0 | **1** | 0 |
| `webhook` (через `admin`) | 2 | **3** | 0 |

То есть socks5 из Alertmanager реально доходит до Telegram — это был главный
неизвестный. В логах `admin` запрос виден уже без токена в URL.

Образ `admin` пересобран и раскатан: `/api/health` = 200 через 25 секунд,
`RestartCount=0`, планировщик стартовал, fail-closed присутствует в работающем
образе. Проверка авторизации после пересборки: верный Basic → 200, без токена → 403.

**Проблема.** Единственный receiver — webhook на `http://admin:8101/api/monitoring/alertmanager/webhook`.
Цепочка `Prometheus → Alertmanager → admin → Telegram` завязана на тот самый сервис, о падении
которого надо сообщать (правило `FrontierAdminDown`, `alerts.yml:531`). Замер:
`increase(alertmanager_notifications_failed_total{integration="webhook"}[7d])` = **135** при
`increase(alertmanager_notifications_total[7d])` = **296**, то есть 46% провалов. По дням:
31.07 — 129, 01.08 — 6, ровно окно Redis-OOM, когда `admin` лежал.

**Якоря:** `prometheus/alertmanager.yml` (receivers: только `blackhole` и `telegram-admin`),
`prometheus/alerts.yml:531`.

**Попутно, тем же файлом:** токен вебхука ездит в query string и пишется открытым текстом
в docker-логи `admin` (`POST /api/monitoring/alertmanager/webhook?token=…`), ротации логов
на демоне нет. И `admin/backend/routers/monitoring.py:63-67` — `if not expected: return`,
то есть при пустой переменной единственный контроль доступа молча отключается вместо отказа.

**Готово, когда:** остановленный `admin` не мешает алерту дойти (проверить руками),
`notifications_failed_total` не растёт, тишина watchdog замечается.

---

### Заход 2 — восстановление из бэкапа — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Скрипт восстановления — `scripts/restore-stack.sh`
- [x] Верификация дампа в `backup-stack.sh` (`pg_restore --list` после снятия)
- [x] Учение: развернуть последний бэкап и сверить контрольные числа
- [x] Метрики и алерты на бэкапы и квоту S3

**Учение пройдено. RTO для БД — 84 секунды** на дамп 580 МБ. `pg_restore` завершился
с кодом 0, схема восстановилась целиком (23 таблицы из 23), данные на месте:

| Таблица | Прод | Восстановлено | |
|---|---|---|---|
| `tables` | 23 | 23 | совпало |
| `workspaces` | 6 | 6 | совпало |
| `sources` | 247 | 247 | совпало |
| `posts` | 313 119 | 311 260 | +1859 после дампа |
| `post_enrichments` | 663 481 | 658 004 | +5477 после дампа |
| `trend_clusters` | 378 | 370 | +8 после дампа |

Учение разворачивает дамп во **временный** postgres на отдельном томе и сносит его
за собой — прод не затрагивается ни на одном шаге. Критерий сознательно не «числа
равны»: прод продолжает наполняться после снятия дампа, поэтому равенства требуют
только справочные таблицы, а для растущих проверяется `0 < восстановлено ≤ прод`.

Режимы скрипта: `verify` (только целостность, безопасно для cron), `drill` (учение),
`postgres|qdrant|neo4j|secrets --confirm-production` (настоящее восстановление,
без флага откажется), `fetch` (скачать из S3, если локальной копии уже нет).

**Что всплыло по дороге.**

*Архив neo4j нечитаем обычным пользователем.* `neo4j_data.tar.gz` пишет контейнер,
файл получается `root:root` с правами 600 — восстановление из-под `ilyasni` не
прочитало бы его вовсе. Режимы, которым он нужен, теперь честно требуют `sudo`.

*Собственный баг в уборке за учением.* `trap` на EXIT срабатывает уже после выхода
из функции, где переменные объявлены как `local`, — уборка падала на
`cname: unbound variable`, и временный контейнер с томом оставались жить. Поймано
проверкой после первого прогона, а не рассуждением; имена переехали в глобальные.

> **И одна ошибка ровно того класса, ради которого всё затевалось.** Правило
> `FrontierS3QuotaCritical` в первой редакции **не могло сработать никогда**:
> `sum(...)` сбрасывает все метки, а левая часть сохраняет `instance`/`job`,
> поэтому сравнение двух векторов не находит ни одной пары рядов и молча даёт
> пустоту. Условие при этом было истинным. Поймано проверкой выражения на живых
> данных — если бы я поверил тому, что правило «загружено и валидно», оно молчало
> бы вечно. Лечится `scalar()`.

**Срочное, обнаруженное этим же заходом.** Бакет заполнен на **87.9%**: занято
13.19 GiB из 15.00, свободно 1.81 GiB, а один суточный бэкап весит 3.56 GiB.
**Места на следующий ночной прогон не хватает.** Разбивка: `backups/` 10.6 GiB
(три суточных набора по 3.5), `media/` 2.0 GiB, `crawl/` 0.5 GiB. Внутри бэкапа
68% — снапшот Qdrant `frontier_docs` (2.4 GiB).

Причина арифметическая, а не случайная. Expiration в S3 отрабатывает асинхронно,
примерно раз в сутки, поэтому при сроке N суток в бакете в пике лежит **N+1**
набор. При 2 сутках это три набора по 3.5 GiB = 10.6 GiB, и вместе с `media/`
и `crawl/` упирается в потолок ровно в момент выгрузки нового.

**Решение владельца (2026-08-04): срок хранения `backups/` снижен с 2 суток до 1.**
В пике останется два набора (~7.1 GiB), заполнение ~64%. Глубина хранения почти
не страдает: локально `backup-stack.sh` держит 7 суток, сокращается только
offsite-глубина. Политика применена (`scripts/s3_lifecycle_apply.py --apply`),
`DESIRED_RULES` в скрипте обновлены, чтобы декларация и бакет не разошлись.

> **Но место освободится не мгновенно.** Подметание по lifecycle у провайдера
> асинхронное: сразу после применения все три набора остались на месте, занято
> по-прежнему 13.19 GiB. Значит ночной прогон **может упасть на выгрузке ещё раз**,
> если подметание не успеет до 03:30. Последствие ограниченное — локальная копия
> снимается в любом случае, теряется только offsite за одну ночь, — и теперь оно
> перестало быть незаметным: `FrontierBackupRunFailing` и `FrontierS3QuotaCritical`
> об этом сообщат. Гарантированный способ закрыть окно — удалить набор
> `backups/2026-08-02/` вручную, он и так уже просрочен по новой политике.

**Проблема.** Бэкапы идут исправно: cron 03:30, последний прогон `backup OK 2026-08-04_033001`,
6 файлов (postgres.dump 607 MB, neo4j_data.tar.gz 629 MB, два снапшота Qdrant, secrets_config,
MANIFEST) в `s3://bucket-467940/backups/`. Обратного пути нет целиком: в `scripts/` ни одного
файла с `restore` в имени, в `docs/` ни одного раннбука восстановления, в самом
`backup-stack.sh` ни одного шага проверки дампа, WAL-архивации у postgres нет, учение
не запланировано нигде.

**Смежный риск, который вылезет ровно здесь:** квота S3 почти выбрана — 13.1 GiB из ~15 GiB,
три дня бэкапов по 3.5–3.6 GiB, то есть свободного места меньше одного суточного бэкапа.
Метрики и алерта на бэкапы нет ни одного из 56 (`grep -iE 'backup|s3|restore' prometheus/alerts.yml`
→ пусто). Механизм для дешёвой метрики уже развёрнут: node-exporter смонтирован с
`./prometheus/textfile` и через него уже живёт `frontier_analysis_last_update_timestamp_seconds`.

**Готово, когда:** восстановление проведено на живых данных, время восстановления измерено
и записано, свежесть бэкапа стала метрикой с алертом.

---

### Заход 3 — потолок 400 постов в кластеризации — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Решить: снять потолок, поднять его или задокументировать как осознанную выборку
- [x] Метрика покрытия + алерт на её падение

**Диагноз оказался точнее исходного.** «29% корпуса» из первой редакции отчёта —
величина по всей истории, а кластеризация по построению смотрит только в окно
(`max(semantic_window, trend_window)`). Значимая цифра — покрытие В ОКНЕ, и оно
показало, что проблема не общая, а адресная:

| Воркспейс | Потолок | Поток/сутки | Покрытие окна |
|---|---|---|---|
| `disruption` | **400 (дефолт)** | ~1000 | **40.7%** |
| `ai_trends` | 550 | 248 | 95.7% |
| `ai_research` | 500 | 288 | 94.7% |
| `ai_products_media` | 500 | 53 | 98.6% |
| `design` | 650 | 60 | 95.4% |

То есть потолки настраивали под объём каждому — **кроме самого крупного
воркспейса**, который так и остался на дефолтных 400 из `shared/config.py`.
При кроне раз в сутки и выборке «N самых свежих» в кластеризацию попадали
примерно последние девять часов ленты `disruption`.

**Сделано:** `disruption` получил `semantic_cluster_max_posts: 2000` — двукратный
запас к потоку, тот же порядок, что у соседей (у них 1.7–2.2× к своему).
Backfill не понадобился: окно скользящее, поэтому при полном покрытии новых
постов старые некластеризованные выкатываются из него сами за 30 суток.

Замер: ручной прогон занял **673 секунды**, штатный ночной по `cluster_runs` —
**523 секунды** против **37** при потолке 400. Кластеров 1636 против 323,
трендов 20 против 4, покрытие сразу 40.7% → 44.5% и продолжит расти.

> **Поправка к первой редакции этого раздела.** Здесь стояло «пиковая память
> worker в пределах лимита 2 ГБ» — указано не на тот контейнер. Джоб уходит
> в `_run_job_subprocess` → `python -m admin.backend.manual_jobs`, то есть он
> ребёнок **admin** и живёт в его cgroup (лимит 4 ГБ), а не worker. По этому
> рунбуку память искали бы не там, где она тратится.
>
> **И запас меньше, чем казалось.** Таймаут дочернего процесса — 900 секунд
> (`_DEFAULT_JOB_SUBPROCESS_TIMEOUT_SEC`, `ADMIN_JOB_SUBPROCESS_TIMEOUT_SEC`
> в `.env` не задан). Было 37с из 900 — 4% бюджета; стало 523с — 58%. Рост
> квадратичный по матрице попарных косинусов, и прогон стартует в 03:35,
> внутри окна ночного бэкапа, на хосте с тремя ядрами. При превышении джоб
> не деградирует, а получает SIGTERM и через 5 секунд SIGKILL, и воркспейс
> остаётся без кластеризации до следующих суток. Поднимать потолок дальше
> без подъёма таймаута нельзя.

**Метрика вместо обещания.** `frontier_clustering_coverage_ratio{workspace}`
и `frontier_clustering_eligible_posts{workspace}` пишет уже стоявший в кроне
`export-analysis-freshness.sh` (окно берётся из конфига каждого воркспейса).
Алерты ловят **регрессию, а не низкое значение**: `FrontierClusteringCoverageFalling`
требует одновременно `< 0.9` и падающую производную. Абсолютный порог тут не
годится — `disruption` сейчас 44% и растёт, и правило по порогу выло бы месяц
подряд на здоровом процессе. Плюс страховочный `FrontierClusteringCoverageCritical`
на `< 0.3` сутки.

> **Грабля, стоившая одного ложного «успеха».** `config/*.yml` **запечён в образ**,
> а не смонтирован. Правка `workspaces.yml` локально + rsync не меняет ничего:
> `bootstrap_workspaces_from_config()` читает файл ИЗ ОБРАЗА. Первый прогон
> bootstrap отрапортовал `{"status":"ok","bootstrapped":[...disruption...]}`
> и записал `disruption` пустой `extra` — из устаревшего файла. На диске, в
> образе `admin` и в образе `worker` лежали три РАЗНЫЕ версии `workspaces.yml`.
> Обход без пересборки: `docker cp config/workspaces.yml <контейнер>:/app/config/`,
> затем bootstrap. Проверять по `md5sum`, а не по отчёту bootstrap.

**Проблема.** `semantic_clustering.py:2486-2494` берёт `limit=max(semantic_cluster_max_posts, 50)`,
где `semantic_cluster_max_posts=400` (`shared/config.py:542`, ни в `.env`, ни в
`admin_runtime_settings` не переопределён). Крон раз в сутки: `ADMIN_SEMANTIC_CLUSTER_CRON='35 3 * * *'`.
`_fetch_posts` (`:491-522`) берёт `ORDER BY published_at DESC LIMIT 400` из 30-дневного окна.

Замер: за сутки в `disruption` подходящих постов 1086, кластеризовано ровно 400. Накопительно
по всему корпусу — 55 308 из 187 944 = **29%**. По месяцам публикации проседание видно ясно:
2026-05 — 6.4%, 2026-06 — 13.6%.

Ни backfill-джобы, ни метрики покрытия, ни алерта нет — то есть даже приняв потолок сознательно,
вы не увидите, когда он начнёт резать больше.

**Готово, когда:** доля кластеризованных от подходящих стала метрикой, у неё есть порог,
а выбранное значение потолка записано с обоснованием.

---

### Заход 4 — переписать `docs/README.md` по коду — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Раздел схем PostgreSQL
- [x] Раздел коллекций Qdrant
- [x] Раздел графа Neo4j
- [x] Блок xray-профилей и «XRAY Runtime Operations»
- [x] Снять пометку ⚠️ из шапки

Переписано двенадцать разделов: БД, Qdrant, Neo4j, Current Runtime, xray, проверка HTTP,
Концепция Workspace, Статус реализации, Архитектура, Технологический стек, MCP Tools,
Processing Pipeline, Admin UI, профили compose, структура репозитория, As-Built Runtime.

**Принцип переписывания — не чинить копию, а убрать её.** Схема БД, дерево каталогов
и списки файлов больше не дублируются: вместо них назначение, указание на источник правды
и команда для сверки. Прежний раздел БД был рукописным псевдо-DDL с несуществующими
таблицами `post_media_map` и `media_group_items`, `sources.type`/`enabled`/`schedule`
вместо реальных `source_type`/`is_enabled`/`schedule_cron` и `UUID PRIMARY KEY` вместо
`TEXT` — по нему нельзя было написать ни одного рабочего запроса. Копия расходится
с оригиналом всегда, вопрос лишь в сроке.

Заодно убран **третий** пересказ конвейера: «As-Built Runtime» дублировал «Архитектуру»
и «Processing Pipeline» и разошёлся с обоими. Осталась таблица «что нужно → где смотреть».

Там, где прежний текст врал, поставлена пометка-предупреждение с доказательством —
чтобы читатель, помнящий старую редакцию, понимал, что именно изменилось, а не решил,
что ему померещилось.

> **Скептический проход снова окупился.** Пять разделов писали агенты, и проверяющие
> нашли в черновиках десятки ошибок: команда сверки числа инструментов печатала `1`
> вместо `32` (`/tools` отдаёт объект, а не массив); `grep -rl` без `--include=*.py`
> считал `.pyc` в `__pycache__`; vision-роутинг был описан по недостижимой ветке кода
> (при наличии wormsoft среди кандидатов порядок жёсткий, и `elif` по `quality_tier`
> не выполняется — по данным за последние 300 обогащений вызовов GigaChat ноль);
> у `concepts` и `valence` не оказалось polza-фолбэка, который им приписали.
> Все исправлены до вставки.

**Проблема.** 35 расхождений — подробности в разделе 3 этого отчёта. Самое опасное не то,
что числа устарели, а что раздел схем описывает таблицы, которых не существует
(`post_media_map`, `media_group_items`), и колонки под другими именами — по этому документу
нельзя написать ни одного рабочего запроса.

**Готово, когда:** каждое утверждение раздела проверено против `storage/postgres/init.sql`,
живого `\dt`, `GET /collections`, `CALL db.labels()` и `runtime/xray-profiles.json`.
Пометка снимается только после этого.

---

### Заход 5 — три невалидных набора профилей — ✅ ВЫПОЛНЕН 2026-08-04

- [x] `scripts/server-deploy-rebuild.sh`
- [x] `Makefile ALL_PROFILES`
- [x] Рецепт `--profile monitor` в `docs/monitoring-runtime-dashboard.md`
- [x] Единое определение вместо пяти копий + проверка перед деплоем

**Починка не в трёх наборах, а в том, что их было пять.** Наборы повторялись в каждой
точке входа и разъехались. Теперь они объявлены один раз в **`scripts/compose-profiles.sh`**,
оттуда их читают `server-build-stack.sh`, `server-deploy-rebuild.sh` и `Makefile`.

Там же функция `frontier_assert_profiles`: скрипты сборки проверяют набор **до** первой
команды. Раньше невалидность вылезала через несколько минут ожидания pip, когда
откатываться дороже. Проверить все наборы разом, ничего не запуская — `make check-profiles`.

| Набор | Было | Стало |
|---|---|---|
| `server-deploy-rebuild.sh` | `core,worker,mcp,crawl,paddleocr` → падал на `crawl4ai depends on undefined service "xray"` | `FRONTIER_PROFILES_BUILD`, 14 сервисов |
| `Makefile ALL_PROFILES` | `core,ingest,worker,mcp,admin` → падал на `ingest depends on undefined service "xray"` | `FRONTIER_PROFILES_FULL`, 18 сервисов |
| `.env.example` (пример) | `core,ingest,worker,crawl,paddleocr,mcp,admin` → падал на `admin` | полный набор с пояснением |
| `--profile monitor` | падал на `alertmanager depends on undefined service "admin"` | **починился сам** в заходе 1 — зависимость снята, чтобы alertmanager не ждал сервис, о падении которого сообщает |

В `server-deploy-rebuild.sh` заодно сняты инлайновые `--profile`: они дублировали набор
третьим местом и точно так же не включали `xray`. `COMPOSE_PROFILES` действует на всю команду.

> **Побочная находка: двойная кодировка в 14 shell-скриптах.** Комментарии в них были
> прочитаны как cp1251 и переписаны обратно в UTF-8, превратившись в нечитаемое
> `РџРѕСЃР»Рµ rsync`. Скрипты при этом исполнялись — страдала только читаемость, поэтому
> дефект жил незамеченным. Исправлено построчно (`encode('cp1251').decode('utf-8')`):
> 43 строки в 14 файлах. Построчно, а не целиком, потому что файлы смешанные — часть
> строк уцелела и содержит символы вроде `→`, которых в cp1251 нет, и попытка починить
> файл целиком падала на них, оставляя битым весь файл. После правки все 36 скриптов
> проверены на синтаксис.

**Проблема.** Все три падают на `docker compose config` с
`service "…" depends on undefined service "xray": invalid compose project` — потому что
`ingest`, `admin` и `crawl4ai` объявляют `depends_on: xray`, а профиль `xray` в наборы
не включён. При этом `docs/ops-server-troubleshooting.md` §5 подаёт `server-deploy-rebuild.sh`
как основную практику деплоя правок кода.

Рабочий эталон один — `scripts/server-build-stack.sh`.

**Готово, когда:** `COMPOSE_PROFILES=… docker compose config --services` проходит у всех
точек входа, а лишние либо приведены к эталону, либо удалены.

---

### Заход 6 — контур одобрения RSI — ✅ ВЫПОЛНЕН 2026-08-04

- [x] Вывести десять инструментов RSI в шлюз
- [x] Контрактный тест против повторного расхождения
- [x] `mcp-gateway` добавлен в список сборки

**Что было.** Десять инструментов (`mcp/tools/graph_health.py` и
`mcp/tools/threshold_proposals.py`) работали по REST, но не были выведены в шлюз,
а единственный клиент ходит только через шлюз. Контур самоулучшения был построен
целиком и не мог сделать ни одного оборота: 86 предложений слияния висели
`pending` с 07.07 при последнем одобрении 05.07, у всех 26 911 строк
`relevance_decisions` поле `audit_status` осталось `NULL` за всю историю.

**Стало:** шлюз отдаёт 32 инструмента против прежних 22 — ровно столько же,
сколько REST. Проверено живым MCP-хендшейком; вызов `list_entity_merge_proposals`
вернул настоящие данные, первое же предложение — `MMAO` против
`Metabolic Multi-Agent Optimizer (MMAO)`, тот самый случай «акроним против
расшифровки», ради которого контур и писался.

**Против повторения — контрактный тест** `tests/test_mcp_gateway_contract.py`:
сравнивает множества имён REST и шлюза в обе стороны и отдельно проверяет
десять имён контура одобрения. Разбор идёт по AST, а не регулярками: декораторы
многострочные, и текстовый поиск их пропускает — на этом я и ошибся при первом
подсчёте, получив 17 вместо 32.

> **Попутно найдено: `mcp-gateway` не входил в список сборки.** У него отдельный
> образ из `mcp/Dockerfile.gateway`, а `DEFAULT_SERVICES` в
> `scripts/server-build-stack.sh` его не перечислял — штатный деплой не обновлял
> шлюз никогда. Это и объясняет находку аудита про устаревший `shared/config.py`
> у шлюза. Обнаружилось буквально: первая сборка собрала образ `mcp`, а шлюз
> остался с 22 инструментами. Добавлен в список.

---

## Маршрут II — заходы 7–12 (построен 2026-08-04 ночью)

Маршрут I (заходы 0–6) пройден целиком. Этот собран по 45 оставшимся открытым пунктам
реестра, каждый из которых перед планированием **перепроверен на живом стеке заново** —
утренние числа за день успели устареть, а часть пунктов починилась побочно.

> **Как этот маршрут проверялся.** Восемь агентов перепроверяли пункты по слоям,
> скептик получал задание опровергнуть каждый вердикт «починено», два критика —
> полноты и исполнимости — разбирали готовый план. Критики нашли существенное:
> семь пунктов были «отложены» только в свободном тексте и при приёмке читались бы
> как забытые; шесть критериев готовности оказались непроверяемы; одно условие
> приёмки было физически невыполнимо (метрики шлюза требовались с процесса, где их
> нет). Всё перечисленное учтено ниже — план в этой редакции уже с правками.
>
> Отдельно стоит записать грабли самого прогона: пять агентов из шести в первом
> заходе довели измерения до конца и были убиты лимитом **ровно перед записью
> результата**. Работа спаслась только потому, что её вытащили из транскриптов.
> Урок на будущее: у агента-исследователя должен быть явный бюджет вызовов и
> инструкция «выдай неполный ответ, но выдай».

### Общая логика порядка

**Сначала чинится канал, потом в него добавляется сигнал.** Заходы 8–12 производят
метрики и алерты, но канал доставки сейчас неисправен по трём независимым причинам:
ежедневная петля разбора алертов умирает второй день подряд и о своей смерти не
сообщает ничем; один алерт кричит шесть суток из семи на штатном суточном сбросе
квоты; инструмент переобработки окна получает 401 и молча ничего не делает, оставаясь
при этом внешне исправным в dry-run. **Сломанный инструмент восстановления хуже
отсутствующего** — поэтому заход 7 идёт раньше «предусловия видимости».

**Потом видимость.** Заход 8 — предусловие честной проверки всего остального: пока
нет `rate()` по стадиям, «починил» в заходах 9–12 нечем подтвердить, а рост потока
в заходе 11 без наблюдения просто опасен — ровно этот сценарий 13–14.07 выжег
кредитное окно wormsoft.

**Связки не разрываются:** 8+37+53 (один контур crawl4ai и одна причина накопления
призраков), 12+39 (обе метрики отдаёт admin из одной точки), 25+26 (в жёстком порядке
26→25), 32+46 и 27+45 (обе пары уехали в отказ одной записью).

---

### Заход 7 — вернуть себе триаж · пункты 44, 18, 56, 15, 30 · S+M / риск низкий

**Цель.** Каждый уже раскатанный механизм обнаружения либо работает, либо честно
помечен мёртвым. Пересборок образов в заходе нет — только скрипты, cron и `alerts.yml`.

- [ ] **44 · `server-reprocess-window.ps1` не переобрабатывает ничего.** Дефект глубже
      401: в боевой ветке (`:52`) стоит `ssh $Server "curl -fsS -X POST ..." | Out-Null`
      **без проверки `$LASTEXITCODE`**, а в PS 5.1 ненулевой код нативного exe не бросает
      исключение даже под `$ErrorActionPreference='Stop'`. Строка `reprocess ok N/N`
      печатается независимо от 401, 500 и таймаута. Поэтому добавить Basic-авторизацию
      **мало** — это чинит сегодняшний симптом и оставляет механизм молчания. Нужны оба:
      проверка кода возврата с падением скрипта и авторизация.
      Креды не подставлять локально: `curl` исполняется внутри `ssh`, поэтому `-u user:pass`
      попадёт в командную строку удалённого процесса и в историю PSReadLine. Рабочая форма —
      один ssh-сеанс, который сам читает значение из серверного `.env` и отдаёт `curl`
      через `-K -` на stdin; заодно свернуть двадцать отдельных ssh-рукопожатий в один
      удалённый цикл. Если в `.ps1` добавляются кириллические комментарии — файл обязан
      быть **UTF-8 с BOM**, иначе PS 5.1 прочтёт его как ANSI (уже ломало `sync-pull.ps1`).
- [ ] **18 · петля alert-triage мертва.** Разделить на две части, потому что у них разная
      природа. *Поставка:* петля обязана сигналить о собственной смерти — метрика свежести
      дайджеста в `prometheus/textfile` плюс правило `FrontierAlertTriageStale` (нет
      дайджеста больше 26ч). Писать **атомарно** (`.tmp` + `mv` на сервере), иначе
      node-exporter отдаст parse error вместо метрики; серию бутстрапить, чтобы алерт не
      оказался вечно зелёным на отсутствии данных. Риска с `sync-push --delete` нет:
      `prometheus/textfile/` уже исключён (`.rsync-exclude:67`). *Исследование:* причина
      `0xC000013A` (STATUS_CONTROL_C_EXIT) — это убийство процесса консольным сигналом,
      смотреть надо настройки самой задачи Task Scheduler («Stop the task if it runs longer
      than», «Run whether user is logged on», закрытие сессии), а не только логи. Разбор
      причины не имеет проверяемого исхода и потому **не является критерием готовности** —
      иначе «дайджест пришёл два дня подряд» закроет пункт случайно.
- [ ] **56 · алерт, приучающий не смотреть.** **Первым делом проверить экспозицию:**
      вариант «исключить карантин, объяснимый суточным сбросом» выразим в PromQL только
      если `quarantine_until` отдаётся как timestamp-гейдж или метка. Если в экспозиции
      только счётчик моделей — пункт тянет правку кода `admin` и пересборку, то есть
      уезжает в заход 8, а здесь остаётся временный silence. Развязку выбирает владелец
      (см. §8). Тест обязан проверять, что **каждое имя метрики из нового `expr`
      присутствует в живой экспозиции** — иначе шумящий алерт заменится вечно зелёным.
- [ ] **15 · экспортёр свежести покрывает 2 таблицы из 5.** Расширить `UNION ALL` на
      `trend_clusters` (`max(updated_at)`) и `missing_signals` (`max(created_at)`); гнать
      цикл не по результату `GROUP BY`, а по `workspaces w LEFT JOIN <таблица>` — иначе
      воркспейс без строк даёт не «протухло», а пустоту, на которой `max by (table)` не
      считается и алерт не сработает никогда. Второй гейдж `frontier_analysis_rows_total{table,workspace}` —
      единственный способ покрыть `card_feedback`, где timestamp взять физически неоткуда
      (0 строк). **Циклов в скрипте два**, и второй (`:90`) отбрасывает воркспейс через
      `continue` при `eligible=0` тем же способом: новый гейдж эмитить **вне** фильтрующего
      `while`, иначе починка воспроизведёт ровно тот дефект, ради которого затевалась.
      Ноль по `card_feedback` ожидаем и алерта на него нет — записать это строкой контракта,
      иначе через месяц прочитают как поломку.
- [ ] **30 · `security-git-preflight.md` описывает две смены auth posture назад.**
      Обновить снимок: `mcp` ушёл на loopback, `admin` закрыт Basic-auth, 8102 остался
      открытым и с захода 6 несёт пишущие RSI-инструменты. Плюс дешёвый статический тест:
      таблица портов в документе сверяется с published-портами `docker-compose.yml` —
      иначе снимок протухнет на следующей же смене.

**Тесты.** `tests/test_alert_rules.py` (unit): `FrontierAnalysisStale` существует с матчером
`table="trend_clusters"` и не разваливается на `absent()`; `FrontierAlertTriageStale`
присутствует; переписанное правило карантина парсится и не ссылается на метки, которых нет
в экспозиции. `tests/test_analysis_freshness_export.py` (integration): эталонный вывод
экспортёра парсится `prometheus_client.parser.text_string_to_metric_families`, содержит серии
`trend_clusters` по **всем шести** воркспейсам (включая `auto_hmi` с нулём) и
`frontier_analysis_rows_total{table="card_feedback"}=0`.
`tests/test_ops_scripts_contract.py` (unit, статический разбор по образцу
`test_alerts_workspace_coverage`): каждый скрипт в `scripts/`, обращающийся к `/api/` админки,
несёт заголовок авторизации **и проверяет код возврата**; ни один `scripts/*.sh` не содержит
CRLF (`set -o pipefail` падает на сервере).

**Готово, когда:** боевой (не dry-run) прогон `server-reprocess-window.ps1` на окне в 20 постов
ставит задачи в стрим, а искусственный 401 роняет скрипт с ненулевым кодом; в
`prometheus/textfile` появились серии `trend_clusters` по всем шести воркспейсам и
`card_feedback rows=0`; за сутки карантин моделей фактически случался (метрика > 0 хотя бы
в одном скрейпе), а алерт при этом молчал; дайджест alert-triage пришёл два дня подряд,
а искусственный пропуск даёт `FrontierAlertTriageStale`.

---

### Заход 8 — счётчики собственных стадий и хранилищ · пункты 13, 14, 12, 39, 24 · L / риск низкий

**Цель.** У каждой стадии пайплайна и каждого хранилища есть серия в Prometheus,
а отсутствие данных выражается **нулём, а не отсутствием серии**.

Общий класс всех пяти пунктов один: метрика либо не объявлена, либо объявлена и умирает
по дороге. Поэтому 39% дропа, всплеск ошибок (29 из 32 всех ошибок случились за последние
сутки — прямо сейчас, и этого не видит ни один дашборд) и 37 426 вытесненных непрочитанными
событий существуют в системе, но невидимы.

- [ ] **Проба перед планированием.** `docker exec` в контейнер `redis` может быть запрещён
      политикой хоста (это отдельно документировано в `CLAUDE.md`), а cron пойдёт от
      конкретного пользователя — проверить, что он в docker-группе. Это единственный пункт
      захода, чей объём неизвестен до проверки прав. Проба ставится **первым шагом**, а не
      посреди работы.
- [ ] **13 · нет счётчиков стадий.** Один `Counter frontier_pipeline_stage_total{service,stage,workspace,outcome}`,
      а не восемь имён из реестра: кардинальность ~6×6×4 = 144 серии, для 22Gi это ничто.
      Инструментировать четыре точки: `ingest/sources/base.py` на `xadd`, worker
      `_update_indexing_status` (здесь главная дыра), vision-таск, reindex/qdrant upsert.
      Проверить, доступен ли `workspace_id` в `_update_indexing_status`; если не пробрасывается —
      либо протащить аргументом, либо метку на этой стадии не ставить **и записать это**,
      а не молча потерять разрез. Границу с `frontier_crawl_outcomes_total` из захода 9
      описать явно, иначе получится либо двойной счёт, либо дыра на самой отказной стадии.
- [ ] **14 · хранилища не скрейпятся.** Экспортёры-контейнеры **не ставить** — `docker pull`
      с этого хоста падает по I/O timeout, Docker Hub недоступен. Self-metrics тоже нет:
      `neo4j:5.15-community` отдаёт `/metrics` только в Enterprise, у `redis`/`postgres` alpine
      эндпоинта нет в принципе. Оба канонических пути закрыты — идти третьим, уже трижды
      обкатанным: `scripts/export-storage-metrics.sh` по образцу `export-analysis-freshness.sh`.
      Каждому блоку свой `_up` gauge, чтобы «ноль» отличался от «не смогли опросить»; писать
      атомарно; LF, не CRLF; `.env` целиком не sourc'ить. Периодичность развести: redis+postgres
      раз в 5 минут, neo4j раз в 10–15 — `cypher-shell` поднимает JVM-клиент на каждый вызов,
      на трёх ядрах это постоянный налог.
- [ ] **12+39 · DLQ и потеря событий при тримминге.** Обе метрики отдаёт `admin` из одной
      точки, рядом с `set_redis_stream_metrics`. `frontier_redis_dlq_length{service,stream}`
      с **обязательным нулём** для несуществующего ключа (сейчас DLQ-ключей нет вовсе, и
      «пусто» неотличимо от «сломано») плюс `frontier_redis_stream_groups{stream}` и детектор
      потери по инварианту `last-delivered-id < recorded-first-entry-id`. На `lag` полагаться
      нельзя: Redis отдаёт NULL, когда тримминг ломает учёт. `MAXLEN` не трогать — у стримов
      с группой 5–10 суток запаса.
- [ ] **24 · RSI-метрики не отдают ни одной серии.** Сеттеры зовутся только внутри дочернего
      процесса `manual_jobs`, у которого свой `REGISTRY` и никакого HTTP. Ни multiprocess
      (`PROMETHEUS_MULTIPROC_DIR` переключает **весь** REGISTRY и ломает живые правила на
      голых гейджах — `alerts.yml:168, :189, :211, :364, :405`), ни textfile (каталог не
      смонтирован в `admin`, а novelty — Counter, монотонность пришлось бы держать руками).
      Третий путь: `_run_job_subprocess` уже возвращает распарсенный JSON ребёнка — разложить
      его по существующим сеттерам **в родителе**. Две предпосылки проверить до работы:
      (а) какую форму отдаёт ответ при `__all__` — если агрегат без разбивки по воркспейсам,
      то `set_graph_health_metric(workspace=...)` нечем размечать и это уже правка контракта
      дочернего процесса; (б) при исключении `manual_jobs` печатает JSON в **stderr** и
      возвращает 1 — родитель, разбирающий stdout, на провальных прогонах не получит ничего,
      то есть метрики не обновятся ровно в тех прогонах, ради которых всё делается. Явный
      `outcome='failed'` по ненулевому коду выхода обязателен.
- [ ] **Дешёвая часть 42, без которой алерт на регрессию не построить.** Ретенция Prometheus
      200ч (8.3 суток) при базовой линии в 7 суток даёт запас 1.3 суток: любой перезапуск
      ломает базу. Либо поднять ретенцию, либо сузить базу до 48ч **и записать это
      ограничение прямо в критерий**. Плюс одно recording-правило под базовую линию —
      долгодиапазонные запросы без него ещё и дороги.
- [ ] **Алерты (девять).** Регрессия доли `outcome=dropped` (на регрессию, не на порог —
      39% это норма проекта); рост `outcome=error` за 1ч; `FrontierRedisMemoryHigh`
      (`used/maxmemory > 0.8` за 10м — это и есть детекция инцидента 31.07);
      `FrontierRedisEvictions`; `FrontierPostgresConnectionsHigh`; `FrontierNeo4jDown`;
      `FrontierStorageProbeStale`; `FrontierDlqNotEmpty`; правило на `groups==0` при растущем
      `entries-added`.
- [ ] **Раскат.** `bash scripts/server-build-stack.sh worker ingest admin` — три образа подряд
      на трёх ядрах при живом стеке, планировать **вне окна ночных джобов**: кластеризация
      `disruption` уже съедает 523–673с из 900с таймаута субпроцесса, и SIGKILL по таймауту
      отравит `done_when` захода 12. После пересборки `admin` проверить его здоровье (образ
      тянет все зависимости `worker.services.*`). Отдельным шагом: **после рестарта `admin`
      RSI-гейджи пусты до следующего прогона джобы** (8–24 часа), а на них висят пять живых
      правил — прогнать три джобы вручную либо приглушить правила на окно, иначе заход выдаст
      себе ложные срабатывания в канал, который заход 7 только что чинил. `alerts.yml` и
      `prometheus.yml` — single-file bind-mount: нужен `--force-recreate prometheus`, не reload.

**Тесты.** `tests/test_metrics_pipeline_stages.py` (unit) — по **всем четырём** точкам
инструментирования, не только `_update_indexing_status`. `tests/test_storage_metrics_export.py`
(integration) — вывод парсится, ветка «опрос не удался» даёт `frontier_redis_up 0`, а не
отсутствие серии. `tests/test_metrics.py` (unit) — снапшот **без** dlq-ключа всё равно создаёт
серию со значением 0. `tests/test_redis_streams.py` (unit, fakeredis) — `entries-added > length`
при пустом `XINFO GROUPS` → `frontier_redis_stream_groups == 0`; отдельный кейс на инвариант
потери. `tests/test_admin_scheduler.py` (unit) — замоканный `_run_job_subprocess` для всех трёх
джобов, **включая `relevance_audit`** (у него в шаге записано ожидание «начнёт честно отдавать
нули» — ожидаемое изменение обязано быть закреплено тестом), плюс ветка ненулевого кода выхода.
`tests/test_alert_rules.py` — все девять новых правил существуют и парсятся. Расширить
`tests/test_alerts_workspace_coverage.py`: каждое имя метрики из `expr` любого правила объявлено
в `shared/metrics.py` **и** имеет хотя бы один вызов сеттера вне `manual_jobs.py`.

**Готово, когда:** за окно T приращение counter по каждому `outcome` равно приросту числа строк
с соответствующим `indexing_status` за то же окно, расхождение не более 5%; `curl` по
`frontier_redis_used_memory_bytes` и `frontier_pg_numbackends` отдаёт серии, а не пустой result;
`FrontierDlqNotEmpty` видит ноль (серия есть, значение 0); детектор осиротевшего стрима
**загорелся** на `stream:posts:enriched` — это и есть доказательство, что он ловит уже
случившееся; `frontier_graph_health` непуст после ближайшего `run_graph_maintenance`.

---

### Заход 9 — краул и консьюмеры · пункты 8, 37, 53, 9 (+ хвосты 40 и 33) · M / риск средний

**Цель.** Тысяча с лишним неудачных попыток краула в сутки перестаёт быть невидимой,
призраки перестают копиться **в корне**, а единственный стрим, реально терявший данные,
исчезает вместе с продюсером.

- [ ] **8 · исходы краула вместо тишины.** `Counter frontier_crawl_outcomes_total{outcome,reason}`,
      `outcome ∈ {saved,empty,failed}`, `reason ∈ {ok,no_results,http_403,http_404,http_other,browser_error,rate_limited,timeout}`.
      Класс `timeout` (366/сут) в прежних замерах не считали вовсе — **именно поэтому доля
      отказов гуляла между 41% и 35%**. Пять `return None` в `enrichment_engine.py:239-292`
      заменить типизированным результатом с `reason`; в `crawl4ai_service.py:96-102` и `:133-136`
      инкрементить перед `XACK` и писать признак «краул не дал контента», чтобы «нечего
      краулить» и «medium отдал 403» перестали быть неотличимы. **Проверить до работы**, что
      `shared/metrics.py` вообще импортируется внутри образа `crawl4ai` (у него свой Dockerfile
      и structlog) и что его `/metrics` действительно скрейпится — иначе счётчики появятся
      в коде и не появятся в экспозиции, то есть повторится дефект пункта 24.
- [ ] **8 · дешёвый выигрыш без метрик.** Отфильтровать хосты соц-шеринг-виджетов
      (`facebook.com/sharer`, `linkedin.com/share`, `twitter.com/intent`, `t.me/share`)
      в `finalize_linked_urls` на стороне ingest — это кнопки «поделиться» со страниц medium,
      порядка 90 отказов в сутки.
- [ ] **37 · уборка мёртвых консьюмеров есть в двух потребителях из четырёх.** Поднять
      `_cleanup_dead_consumers` в общий хелпер, заменить обе дублирующие копии вызовом
      и добавить вызов в `reindex_task` и в потребительский цикл `crawl4ai`. Корреляция
      прямая: где уборка есть — 1 консьюмер, где нет — 85 и 14.
- [ ] **53 · стабильное имя консьюмера.** «Брать из HOSTNAME» чинит меньше, чем обещает:
      у этих сервисов в compose нет ни `hostname:`, ни `container_name`, поэтому HOSTNAME =
      короткий id контейнера. Он переживает `restart`, но **меняется на каждом
      `--force-recreate`**, а их в заходах 8–12 четыре. Нужна явная переменная в compose
      (`CONSUMER_NAME=<service>-1`) либо константа имени сервиса плюс порядковый номер.
      Инвариант в тесте формулировать как «имя переживает force-recreate», а не «одинаковый
      HOSTNAME даёт одинаковое имя» — второй пройдёт и на нерабочем варианте.
- [ ] **9 · осиротевший продюсер.** Снять `STREAM_OUT` и блок публикации. Consumer-группу
      **не заводить**: группа без потребителя перестанет терять данные и начнёт копить
      pending, то есть проблема сменит форму и станет шумом в алертах. Ключ удалить,
      упоминание вычистить из `docs/README.md`.
- [ ] **Записи в Redis — одной согласованной операцией.** `DEL stream:posts:enriched`,
      разовая зачистка 85 накопленных консьюмеров и снятие записанного состояния детектора
      из захода 8 — это три записи в Redis. Свести в одну операцию с явным одобрением;
      иначе после удаления ключа останется висящая серия или вечное срабатывание детектора
      на несуществующем стриме.
- [ ] **Хвост 40 (перенесён из захода 10).** FK-violation в crawl-консьюмере пишет
      reject-статус и делает `xack`, а не уходит в бесконечный reclaim. Это тот же блок
      `crawl4ai_service.py:96-136`, который заход уже переписывает, — экономия одной
      пересборки и один проход по коду вместо двух.
- [ ] **Хвост 33.** Не создавать `media_group` при `media_urls < 2` — несколько строк,
      независимо от остального, и без него будущий фикс пункта 7 ухудшит пункт 33.
- [ ] **Алерт на рестарты.** `changes(process_start_time_seconds{job=~"worker|ingest|crawl4ai|mcp|admin"}[1h]) > 0` —
      метрика уже собирается, пересборки не требует, и это единственное, что реально
      приблизит к причине **следующего** падения crawl4ai: окна данных по 84 прошлым нет
      (ретенция 200ч), `ExitCode=0` не говорит ничего.

**Тесты.** `tests/test_crawl4ai_service.py` (unit): 403 → `reason='http_403'` **и** `XACK`
всё равно вызван; пустой results → `empty/no_results`; успех → `saved`; отдельный кейс на
запись признака «краул не дал контента» — это единственная часть пункта, меняющая данные.
`tests/test_linked_urls.py` (unit): шеринг-URL отбрасывается, обычный остаётся.
`tests/test_reindex_task.py` **и** кейс на потребительский цикл `crawl4ai` (85 призраков из 99
живут именно там): три консьюмера (живой; idle>1ч pending=0; idle>1ч pending=3) → `DELCONSUMER`
ровно один раз для второго. `tests/test_redis_streams.py::test_consumer_name_survives_recreate`.
`tests/test_enrichment_task.py` (unit): в happy-path среди вызовов `redis.xadd` нет ни одного
со стримом `stream:posts:enriched` (assert по списку `call_args`, не по счётчику).

**Готово, когда:** `sum by (reason) (rate(frontier_crawl_outcomes_total[1h]))` воспроизводит
раскладку из `docker logs` с точностью до округления; после `--force-recreate crawl4ai` число
консьюмеров в группе **не увеличивается** (критерий на прирост, а не на абсолютное падение —
разовая зачистка идёт отдельной согласованной операцией); `entries-added` у
`stream:posts:enriched` не растёт при двух замерах с интервалом в час, а детектор осиротевшего
стрима погас, и его серия исчезла, а не занулилась.

---

### Заход 10 — MCP-поверхность · пункты 11, 41, 16 (+ хвосты 32, 46, 52, 43) · M / риск средний

**Цель.** Инструменты перестают читать и писать чужой воркспейс по голому id, синтез
перестаёт ходить в гарантированный 404, поверхность доставки получает собственный счётчик.

> **Переставлен вперёд относительно раската источников.** Скоуп воркспейса — единственный
> пункт маршрута, где возможна **запись** в чужой воркспейс; он дешёвый и ни от чего не
> зависит. Держать его после захода, который включает два десятка источников и наполняет
> воркспейсы, значит всё это время писать по голому id уже по более населённым данным.
> Аргумент «пока поток не раскатан, мерить нечего» относится к телеметрии (16), а не к 11.

- [ ] **11 · скоуп воркспейса.** `assert_row_workspace(row, workspace)` → **404**, а не 403:
      403 подтверждает существование чужого id. Дописать опциональное `workspace: str | None = None`
      в четыре Request-модели (опциональное ради совместимости с текущими клиентами), вызывать
      после `_fetch_one` в четырёх читающих инструментах; отдельно `get_signal_timeline`, где
      гвард зовётся, но кластер тянется `WHERE id = :id` без фильтра. В `mark_relevance_audit`
      и `approve_threshold_change` дописать `workspace_id` в `WHERE` у `UPDATE`.
      **Поверхность шире, чем в реестре:** в шлюзе обёрнуто ~34 инструмента, а не 19, и десять
      из них (graph_health / threshold_proposals) существуют **только** в шлюзе — гвард обязан
      накрывать и эти пути, иначе защита получится частичной ровно на пишущих RSI-инструментах.
- [ ] **41 · синтез гарантированно тратит круг на 404.** Причина однострочная: голый
      `model_override=settings.gigachat_model_pro` на трёх call-site'ах подменяет только `model`
      у первого кандидата семейства (им идёт wormsoft), провайдер остаётся — wormsoft получает
      GigaChat-идентификатор и отдаёт 404 (5 из 5 вызовов). Убрать голый override; если синтезу
      нужна модель посильнее — задавать пару целиком. Защита от повторения: применять голый
      override только при совпадении семейства, иначе `log.warning` и пропуск.
- [ ] **16 · телеметрия инструментов.** Реестр в части «Есть» устарел: `curl 8100/metrics | grep -c frontier_`
      → **0**. Метрики у `mcp` держались не на инструментировании, а на редком побочном эффекте
      походов в LLM, и ретенция их вымыла. `Counter frontier_mcp_tool_requests_total{tool,transport,outcome}`
      и `Histogram frontier_mcp_tool_duration_seconds{tool}` с бакетами до 30с. **Шлюз — отдельный
      процесс с отдельным реестром**, он лишь проксирует в `REST_BASE/tools/{name}`: инструментировать
      его придётся своими счётчиками, а `curl 8102/metrics` физически не может отдать счётчики
      инструментов с 8100. Метка `transport` со стороны `mcp` ненаблюдаема — вызовы от шлюза
      приходят обычным HTTP и неотличимы от прямого REST; нужен явный заголовок от шлюза и
      дефолт `rest`, иначе разрез будет фиктивным. `prometheus_client` в образе шлюза лежит
      транзитивно — прописать в requirements **явно**. Записать в шаге и то, что новый `/metrics`
      на `0.0.0.0:8102` станет публично читаемым в сегменте LAN: решение по 8102 не
      пересматривается (§8), но факт лучше зафиксировать, чем обнаружить потом.
- [ ] **Хвост 43 · Prometheus не наблюдает сам себя.** Добавить job `prometheus` и job
      `mcp-gateway` в `prometheus.yml` плюс правила `up == 0`. Самонаблюдение — то, что делает
      проверяемым «алерт молчит, потому что всё хорошо» против «алерт молчит, потому что
      Prometheus не собирает». Без него все критерии вида «алерт не firing» слабее, чем выглядят.
      Остальная часть 43 (healthcheck у 11 сервисов) — отложена, см. ниже.
- [ ] **Хвосты 32, 46, 52** — сужение контракта, три коммита по несколько строк, та же пересборка.
- [ ] **Вычеркнуть три несуществующих имени метрик** из `.cursor/rules/02-tech-stack.mdc:126`
      и `06-devops.mdc:133`. Держать в контракте то, чего нет, дороже, чем признать отсутствие.

**Тесты.** `tests/test_mcp_guards.py` (unit) — чужой воркспейс → 404, свой → проходит,
`None` → проходит. `tests/test_observability_tools.py` (unit) — по всем четырём читающим
инструментам **и по трём пишущим**: запись в чужой воркспейс — более тяжёлый исход, чем чтение,
и он не может остаться непроверенным. `tests/test_llm_router_client.py::test_bare_model_override_skipped_on_family_mismatch`.
`tests/test_mcp_tool_metrics.py` (unit, `fastapi.testclient`) — middleware не считает сам
`/metrics` и `/healthz`. `tests/test_mcp_gateway_contract.py` — паритет `inputSchema` между
`mcp/server.py` и обёртками шлюза (рассинхрон даёт расхождение Claude Code ↔ Desktop);
`/metrics` шлюза отдаёт **его собственные** счётчики. Статический кейс: ни одно имя метрики
из `.cursor/rules/*.mdc` не отсутствует в `shared/metrics.py`.

**Готово, когда:** `curl` с чужой парой id/workspace даёт 404 по всем четырём читающим и
отказ по трём пишущим; следующий вызов синтеза виден как `{task=mcp_synthesis, provider=wormsoft, status=ok}`
(критерий про «нулевой прирост `frontier_llm_fallbacks_total{reason=...}`» **не использовать**,
пока существование этой метки не подтверждено в экспозиции — отсутствующая серия даёт вечно
зелёный критерий, то есть ровно тот класс, который маршрут объявляет главной находкой);
`/api/v1/targets` содержит job `prometheus` и job `mcp-gateway` со здоровьем `up`.

---

### Заход 11 — источники: раскат волнами · пункты 10, 48, 38 · M / риск средний

**Цель.** Заведённые и ни разу не запускавшиеся источники начинают давать поток
под наблюдением, а массовый reprocess перестаёт молча лишать краула RSS-корпус.

Это единственный заход, наращивающий вход системы, и он идёт после 8–10 сознательно:
прирост потока ориентировочно +30–40%, а ровно этот сценарий 13–14.07 выжег кредитное
окно wormsoft (3M/4ч) и уронил обогащение на медленный polza.

- [ ] **Гейт мощности — первым шагом, а не подразумеваемым.** Кластеризация `disruption`
      уже занимает 523–673с при таймауте субпроцесса 900с. Прирост потока увеличивает число
      подходящих постов окна, и к третьему батчу прогон может начать умирать по SIGKILL —
      это отравит и этот заход, и `done_when` захода 12, который опирается на свежий прогон.
      После каждого батча мерить длительность; при >700с сначала поднять
      `ADMIN_JOB_SUBPROCESS_TIMEOUT_SEC` или снизить потолок, и только потом следующий батч.
- [ ] **Зафиксировать точный список id до написания теста.** В исходных формулировках
      арифметика не сходится: «26 заведённых», «+27 источников», а перечисленные батчи дают
      4+6+4+3+2+2 = 21. Контрактный тест закрепит не тот набор, если не пересчитать по БД.
- [ ] **10 · снять дубли перед включением.** Четыре из девяти выключенных источников `auto_hmi`
      повторяют выключенные источники `disruption` с теми же фидами. Включать оба контура без
      дедупликации нельзя: удвоится и поток, и счёт LLM.
- [ ] **10 · механика включения — три действия, и порядок зависит от пересборки.**
      `docker cp config/sources.yml` живёт **до первого recreate**, а заход сам пересобирает
      `admin` ради `pipeline.py`. Значит порядок: правка YAML → `sync-push` → пересборка `admin`
      → bootstrap. `docker cp` — только для случая «без пересборки». Проверять
      `md5sum /app/config/sources.yml` внутри контейнера; отчёту bootstrap не верить (он уже
      однажды отрапортовал `ok`, перечитав старый файл из образа). Telegram-источники `auto_hmi`
      оставить на вторую волну: у них нет `filters`, весь поток канала уйдёт в LLM.
- [ ] **48 · дальше по одному батчу с суточной паузой** в порядке ценности. Батч считается
      раскатанным, когда по нему появились строки в `source_runs`, а не когда `is_enabled`
      стал `true`. **Снимать базовые значения метрик в файл перед каждой волной:** шесть волн
      с суточной паузой — это 6+ суток, а при ретенции 200ч база первой волны к последней уже
      вымыта, и сравнение «до/после» по тем же сериям физически не построить.
- [ ] **Откат батча — тоже два действия** (`PATCH /toggle` + правка YAML + bootstrap), иначе
      откат слетит на ближайшем bootstrap так же молча, как слетает включение. При выжигании
      кредитного окна откат нужен быстрый и записанный заранее.
- [ ] **38 · восстановление ссылок при reprocess.** Вынести пару `html_to_text` +
      `finalize_linked_urls` в общий модуль и заменить `pipeline.py:183` на порядок:
      сперва HTML из `extra->'raw_payload'`, затем фолбэк на plain-текст. У telegram
      `raw_payload` нет — остаётся только фолбэк. Побочный выигрыш важнее цифры: первичный
      и повторный проход перестают расходиться.

**Тесты.** `tests/test_sources_config_contract.py` — расширить с 10 `auto_*`-id на **весь**
пересчитанный набор шести батчей; сейчас пять батчей контрактом не покрыты вообще и дрейф
YAML↔БД по ним не ловится. Добавить кейс «нет двух включённых источников с одинаковым feed URL» —
защита от возврата дубля при ближайшем bootstrap. Кейс на отсутствие `filters` у telegram-батча
оставить. `tests/test_reprocess.py::test_reprocess_recovers_links_from_raw_payload` плюс парный
кейс: telegram-пост без `raw_payload` не регрессирует.

**Готово, когда:** по каждому раскатанному батчу есть прогоны в `source_runs` за сутки;
`auto_hmi` даёт больше 30 постов/сут; p95 доли `outcome=dropped` за 24ч после волны не выше
базовой линии более чем на согласованное число процентных пунктов (порог зафиксировать
до первой волны); пик расхода wormsoft за скользящие 4ч не превышает согласованной доли
лимита ни в одном окне суток; длительность ночной кластеризации не превысила 700с;
reprocess 20 rss-постов даёт crawl-обогащения (до фикса — ноль).

---

### Заход 12 — провенанс доезжает до поверхностей · пункты 26, 25, 28 · M / риск средний

**Цель.** «Не измерено» перестаёт выглядеть как «полностью синдицировано», независимость
доезжает до Qdrant и брифа, а выдача не отдаёт N копий одного материала как N независимых
подтверждений.

Порядок внутри захода жёсткий: **26 перед 25**. Без маркера «не измерено» поисковые
поверхности начнут отдавать 0.0 у 356 из 397 кластеров и растиражируют ту же двусмысленность
вширь.

- [ ] **26 · маркер «не измерено».** `deduped_source_count = 0` — надёжный маркер: у любого
      посчитанного кластера `source_count >= 1`, значит и `deduped >= 1`. Ни миграции, ни новой
      колонки: отдавать `independence_score = None` вместо 0.0 и добавлять явное
      `provenance_measured`. Бэкфилл 356 исторических строк **не делать** — они вне окна
      принятия решений, а пересчёт потребовал бы поднимать векторы и URL постов той эпохи.
      `None` — это изменение схемы **ответа**: обёртки шлюза и Pydantic-модели должны допускать
      null, иначе двусмысленность сменится `ValidationError`, то есть поломкой.
- [ ] **26 · записать в контракт**, что размеченного набора independent/syndicated в репозитории
      нет — даже у измеренных это индикатор, а не валидированная метрика.
- [ ] **25 · провенанс в payload Qdrant.** Добавить четыре ключа рядом с уже присутствующими
      `source_count`/`source_diversity_score`. Это не «потерялось по дороге», а два независимых
      списка полей, из которых синхронизировали один. Расширить whitelist `frontier_brief`.
      Post-level поверхности (`search_frontier`/`search_balanced`) провенансом **не трогать** —
      там его нет и считать в онлайне дорого; записать это строкой контракта, а не оставлять
      двусмысленностью.
- [ ] **28 · canonical-URL дедуп** поверх гидрированных хитов: `url` уже приходит из payload,
      ходить в Postgres не нужно. Нормализовать (нижний регистр хоста, снять `utm_*`/`fbclid`/
      фрагмент, схлопнуть `www`), сгруппировать, оставить хит с максимальным score, проставить
      `duplicate_count` и список `source_id` перепечаток. **Ставить дедуп до сборки промпта
      синтеза**, а не после: иначе модель по-прежнему увидит N копий и посчитает их
      подтверждениями — цель захода не достигнута при зелёных тестах на составе выдачи.
      Убедиться, что `search_balanced` проходит через ту же функцию, иначе две поверхности
      начнут отвечать по-разному на один запрос.
- [ ] **28 · пометить как не измеренное, а не чинить.** Текстовые перепечатки без общего URL
      в онлайне не ловить — записать в контракт, что дедуп идёт только по canonical URL.
      Точки с пустым `semantic_cluster_id` — отдельная строка реестра про покрытие
      кластеризации, поиском это не чинится.

**Тесты.** `tests/test_provenance.py` (unit): `deduped_source_count=0` → `None` и
`provenance_measured=False`; ненулевой → число и `True`; кейс-инвариант — расчётная ветка
никогда не возвращает 0 при непустом наборе источников, иначе маркер перестаёт быть надёжным.
**Параметризованный кейс по всем семи точкам выборки** — не по одной функции преобразования:
иначе повторится ровно тот дефект «два списка, синхронизировали один», из-за которого возник
соседний пункт 25. Контрактный кейс на обёртке шлюза, что null проходит.
`tests/test_search_contracts.py` (unit): два хита, различающиеся только `utm_source` и `www` →
один хит с `duplicate_count=2`; хиты без url не схлопываются; одинаковый хост и разный путь
остаются раздельными; при `limit=N` после дедупа возвращается не больше N и порядок по score
сохранён; обе поисковые поверхности схлопывают одинаково.

**Готово, когда:** payload trend-коллекции несёт четыре новых ключа (критерий проверять
**на `disruption`**: у низкопоточных воркспейсов прогон может не дать ни одного кластера,
и заход будет выглядеть проваленным; либо использовать существующий
`scripts/sync_trend_clusters_to_qdrant.py` для разового переноса вместо ожидания ночи);
`list_clusters` отдаёт `null` и `provenance_measured=false` у кластеров ранней детекции и
число у августовских; `search_frontier` по запросу с заведомой перепечаткой возвращает один
хит с `duplicate_count` вместо трёх, длина выдачи не превышает `limit`.

---

### Отложено до маршрута III — с обоснованием, а не умолчанием

Кап в шесть заходов — это 20–25 пунктов, а открытых 46. Перечисленное ниже **не забыто,
а отложено**: ни один из этих пунктов не разблокирует другие.

| Пункт | Почему уступил место |
|---|---|
| **7** vision до гейта релевантности | ~38 напрасных вызовов в сутки — деньги, но не поломка. Идёт связкой с 33 и 34: без терминального состояния vision (34) фикс 7 ухудшает 33 |
| **34** `vision_status` непригоден как индикатор | Единственный источник терминального состояния для vision; в одной связке с 7 |
| **6** триггер петли card_feedback | Гейт калибровки в 60 пар недостижим, пока нет второй оси, а ось свёрнута решением (см. отказ по 27) |
| **35** из Neo4j ничего не удаляется | 288 осиротевших `(:Document)`; граф монотонно растёт, но поиск от этого не врёт |
| **42** SLO и recording-правила | Дешёвая часть (ретенция + одно recording-правило) забрана в заход 8; остальное — большая работа без разблокировок |
| **43** healthcheck у 11 сервисов | Две трети пункта. Детекция рестартов забрана в заход 9, самонаблюдение Prometheus — в заход 10. **Пункт закрывается частично, а не целиком** |
| **50** альбомные vision-сводки в MCP | Данные есть и не портятся; поверхность добавляется в любой момент |
| **29** (часть) дефект слияния сигналов | `signals_merged=0` у всех шести арифметически недостижим: максимум similarity 0.50 при пороге 0.72, потому что в сигнальной формуле нет члена косинуса центроидов. Чистый дефект объёма L; вторая половина пункта — решение владельца (§8) |
| **54, 55** гигиена | Уборка веток и образов, десять минут хвостом к любому заходу |

---

## 8. Принятые решения

Записано, чтобы следующая сессия не поднимала вопрос заново.

### 2026-08-04 · MCP-шлюз 8102 остаётся без аутентификации

**Решение владельца: отложено, не актуально.** Обоснование — хост в локальной сети
(`192.168.31.222/24`), наружу не проброшен.

Что остаётся принятым риском: любое устройство в сегменте `192.168.31.0/24` может без
единого credential прочитать всю базу знаний через 22 инструмента шлюза и записать в
`card_feedback`, а также инициировать серверную загрузку через `ingest_url`. Смягчение:
`ingest_url` прикрыт `assert_public_http_url` от SSRF.

Решение стоит пересмотреть, если появится любое из: проброс порта наружу, доступ к сети
через VPN/tailscale с недоверенных устройств, или новый пишущий инструмент в шлюзе
с более серьёзными последствиями, чем запись отзыва о карточке.

> **⚠️ Третье условие наступило в тот же день, заходом 6.** В шлюз выведен контур
> одобрения RSI, и четыре из десяти инструментов — пишущие, серьёзнее всего,
> что было там раньше:
>
> | Инструмент | Что делает |
> |---|---|
> | `approve_entity_merge` | сливает два концепта в графе Neo4j; обратно автоматически не разделяется |
> | `reject_entity_merge` | закрывает предложение |
> | `approve_threshold_change` | пишет новый порог в `workspaces.extra->cluster_analysis`, меняя поведение детектора со следующего прогона |
> | `mark_relevance_audit` | пишет человеческий вердикт в `relevance_decisions` |
>
> Было: запись отзыва о карточке и постановка URL в очередь краула. Стало: правка
> графа знаний и порогов детектора — без единой проверки на поверхности,
> опубликованной на `0.0.0.0:8102`.
>
> Заход 6 выполнен как задача владельца, и контур без «одобрить» бессмыслен,
> поэтому инструменты выведены. Риск был предъявлен владельцу дословно, вместе
> с ценой закрытия: бинд на `127.0.0.1` плюс SSH-туннель у клиента не требует ни
> аутентификации в коде, ни изменения инструментов, но MCP перестаёт работать,
> пока туннель опущен.
>
> **Решение подтверждено 2026-08-04 повторно: оставить как есть.** Формулировка
> владельца — сервис в локальной сети, посторонних в ней нет. То есть вопрос
> обсуждён дважды, второй раз с полным составом пишущих инструментов на руках,
> и оба раза закрыт сознательно.
>
> **Не поднимать заново.** Единственное основание для нового разговора — смена
> сетевого контура: проброс порта наружу либо доступ в эту сеть с недоверенных
> устройств. Само по себе появление новых инструментов в шлюзе таким основанием
> больше не является — этот аргумент уже отработан.

### 2026-08-04 · Что честнее убрать из контракта, чем доделывать

Семь пунктов реестра предлагается закрыть **сужением контракта**. Общее у них одно:
система обещает поведение, которого нет, и обещание дороже отсутствия.

**27 + 45 · ось own_stake — не недоделка, а свёрнутое решение.** Коммит `5d972f5`
(в `main`) прямым текстом переносит корпус автора в отдельный сервис; живая коллекция
удалена вручную 04.08 (5936 точек, 426 файлов), остаток кода назван донором для
будущего сервиса, а мотив в теле коммита — пустая коллекция всё равно попадала
в перечисление ночного бэкапа. Калибровать пороги нечего, пока оси нет. Код
`search_frontier.py:251-413` и `scripts/index_own_corpus.py` **не удалять**; регрессия
уже покрыта `tests/test_own_stake.py`. Следствие: гейт калибровки петли card_feedback
(пункт 6) в нынешнем виде недостижим по построению, и это надо признать, а не ждать.

**32 + 46 · слот `reactive`.** Зарезервирован во всех слоях, за всё время не появилось
ни одного потребителя реактивной полосы. `CHECK` в `init.sql` **оставить** (сужение
требует миграции ради нуля выгоды), снять `'reactive'` из модели и сузить `Literal`
в контракте MCP — чтобы клиент получал `ValidationError` вместо 200 с пустым списком.

**52 · `global_signals`.** Удаление одной строки: это та же ссылка на `main_search['results']`,
потребителей во всём репозитории ноль. Строить настоящую полосу бессмысленно —
`source_region='global'` у 98.1% точек, она совпадёт с main.

**36 · пустые метки графа.** Убрать констрейнты `source_id`/`trend_id` из
`storage/neo4j/schema.cypher`, а не достраивать провенансный граф. `(:Source)` и
`(:TrendCluster)` пусты, `FROM_SOURCE`/`EVOLVED_FROM`/`BRIDGES` не существуют, метки
видны в `db.labels()` только из-за констрейнтов — контракт обещает пять меток при трёх
наполненных. Дорогой вариант не оправдан: провенансный сигнал уже течёт через `ECHO_OF`
(485 рёбер и растёт) и колонки PostgreSQL.

**33 · Album Assembler.** Долг вдвое меньше заявленного: из 270 несобранных альбомов
129 принадлежат dropped-постам и ~114 имеют `item_count < 2`; валидных 82 при приросте
~10/мес. Единственная срочная часть — не создавать `media_group` при `media_urls < 2` —
забрана хвостом в заход 9.

### 2026-08-04 · Открытые развилки — ждут решения владельца

Шесть вопросов, где выбор меняет смысл, а не реализацию. До ответа соответствующие
пункты в маршрут не заводятся.

| Пункт | Вопрос | Варианты |
|---|---|---|
| **56** | Чем гасить ежедневный алерт о карантине бесплатных моделей OpenRouter | (а) исключить карантин, объяснимый суточным сбросом, и алертить на следствие · (б) поднять порог и `for` · (в) снизить до `info`, снять `notify` · (г) оставить как есть |
| **22** | Закрывать ли внешнюю экспозицию: `ufw` inactive, `grafana:3000` и `admin:8101` на `0.0.0.0`, кука без `secure` (часть про 8102 не пересматривается) | увести на loopback + SSH-туннель · `ufw` с allow-list по IP · TLS-терминация · принять как осознанный риск |
| **31** | Строить ли контур «краул → og:image → S3 → vision»: vision обслуживает 1.86% корпуса, при этом 86 652 `og:image` уже лежат мёртвым грузом | пилот на одном воркспейсе (L) · сузить контракт, задокументировав `search_by_vision` как telegram-only (S) · отложить, починив только метрику занятости бакета |
| **29** | Снижать ли структурные минимумы стабильного тренда для пяти низкопоточных воркспейсов (`>=2` semantic clusters, `>=3` docs, `>=2` sources при 94–97% синглтонов) | сначала починить дефект слияния и замерить · снизить минимумы сразу · оставить как есть |
| **47** | `cross_workspace_bridges`: читателя ради поведения нет, но описания инструментов обещают поведение модели-клиенту | перестать обещать — поправить описания (S, рекомендуется) · добавить `include_bridges` с обязательной атрибуцией воркспейса происхождения (M) |
| **49** | email-коннектор: ноль источников за всё время, пароль из git-трекаемого YAML мимо `Settings`, `imaplib` синхронно внутри async, `except: pass` | сузить контракт и убрать тип (S) · доделать целиком · оставить как есть |

> По **31** попутно обнаружен самостоятельный дефект, не зависящий от выбора:
> метрика занятости бакета из захода 2 зажата в `INT32_MAX` из-за `%d` в `awk` —
> реально занято ~9.67 GiB из 15, а метрика показывает 2 GiB. **Алерт на квоту
> не сработает именно тогда, когда понадобится.** Чинится в любом случае.
>
> По **49** третий вариант («оставить как есть») явно хуже двух других: риск не
> в неработающем коде, а в том, что он работоспособен ровно настолько, чтобы
> кто-то включил его одной строкой в YAML.

---

*Метод: три прохода (снимок реальности → сверка документов → скептическая перепроверка),
36 агентов, 1782 инструментальных вызова. Все спорные вердикты перепроверены с заданием
опровергнуть; 11 отменено.*
