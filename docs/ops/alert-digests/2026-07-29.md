## TL;DR
Горит 1 warning (`FrontierRedisStreamOldestPendingTooOld`, enrichment). Не poison — выжжен 4ч-бюджет Wormsoft, enrichment замедлился на медленном фолбэке. Само уйдёт на сбросе окна Wormsoft; действие сейчас не требуется, только наблюдать. Critical нет, targets_down нет.

## Горит сейчас (1)
### FrontierRedisStreamOldestPendingTooOld · warning · admin / enrichment_workers
- **Что**: stream:posts:parsed/enrichment_workers держит pending-сообщение старше ~4830с (порог `oldest_pending_age > 1800s for 15m`). activeAt 05:09 UTC.
- **Данные**: pending=5 (метрика) / в PEL 2 сообщения на живом консьюмере `worker-12f60582`, oldest `times_delivered=1`, idle ~7мин — обрабатывается, не застряло. lag=147. **`frontier_llm_finops_published_remaining{wormsoft}=0`** (бюджет выжжен), fallbacks 15m: wormsoft `guard_quarantine`=45.8, `chain_escalation`=2. polza=0, gigachat=406M, openrouter=1000. Ни одного poison-признака (delivery_count низкий, не растёт).
- **Вероятная причина**: ↻ известный паттерн: `ops_enrichment_throughput`. Утренний булк выжег план Wormsoft (3M/4ч) → роутер фолбэчит на медленный провайдер → enrichment замедлился в разы → backlog стареет >30мин. НЕ рецидив poison (`project_enrichment_poison_pending_fix` держится: times_delivered=1).
- **Действие**: наблюдать — саморазрешается на сбросе 4ч-окна Wormsoft. Если не уйдёт за ~1–2ч или lag начнёт расти — `curl :8101/metrics | grep finops` (ждём wormsoft>0) и рассмотреть переприоритизацию gigachat (406M остатка) выше polza как быстрый фолбэк (риск 429 под concurrency).

## Отгремело за 24ч (3)
- FrontierOpenRouterModelQuarantineBurst (admin) — было, ушло; сейчас `openrouter_models_quarantined=0`, баланс $19.98. Повтора не ждём без нового всплеска ошибок.
- FrontierOpenRouterTextFallbackBurst (worker) — отгремело, тот же цикл фолбэков (Wormsoft burn); сейчас тихо.
- FrontierOpenRouterVisionFallbackBurst (worker) — отгремело; vision-lag=0, стрим чист.

## Чисто
- targets_down: пусто (все up). Все 18 контейнеров Up, admin/worker живы, RestartCount не рос.
- NoNewPosts: не горит. last_post_age: disruption ~1.2ч, design ~2ч, ai_research/ai_trends ~2.25ч, ai_products_media ~3.9ч — в пределах tier-порогов.
- Диск / 31% (64G/216G, 143G свободно), load 1.28. GigaChat здоров (base 255M, Ultra 50M, Pro 41.7M, emb 32.3M, Max 26.4M). SearXNG/graph — без ошибок. throttle=0, rate_limit=0. Scheduler running=1.
