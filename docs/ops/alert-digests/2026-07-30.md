## TL;DR
Горит 2 warning (0 crit): OpenRouter отдаёт 403 по free-модели `gemma-4-31b-it:free` → она в карантине, vision-цепочка (text+vision) спилловерит на polza. Кредиты OR целы ($19.98), пайплайн чист (lag/pending=0, targets_down=0). Действие сейчас не требуется — карантин самозаживающий; наблюдать, что не перерастёт в постоянный отвал.

## Горит сейчас (2)

### FrontierOpenRouterVisionFallbackBurst · warning · worker
- **Что**: vision-фолбэки worker→polza (reason=openrouter_403) выросли на ~8 за 15м; растут стоимость и латентность vision.
- **Данные**: `vision_fallbacks[30m]` reason=openrouter_403 = 8.07 (порог ≥5); остальные reason'ы (credit_cap, catalog_missing, or_error) = 0. Карантин: `frontier_openrouter_model_quarantine{model_id="google/gemma-4-31b-it:free"}=1`, все прочие ~37 моделей = 0. OpenRouter-баланс = $19.98 (норма).
- **Вероятная причина**: free-модель `gemma-4-31b-it:free` отдаёт 403 (типовое для `:free` — временная недоступность/лимит free-tier), guard её карантинит → vision-задача уходит на polza. Не кредиты, не catalog. Родня: `FrontierOpenRouterModelQuarantineBurst` (отгремел за 24ч) — та же причина.
- **Действие**: наблюдать — карантин авто-истечёт при восстановлении free-модели. Если бурст держится >1–2ч или повторяется днями — снять vision-primary с флапающей `:free` gemma и поставить стабильную альтернативу из каталога.

### FrontierOpenRouterTextFallbackBurst · warning · worker (task=vision)
- **Что**: text-фолбэки для worker/vision worker→polza (reason=openrouter_403) выросли на ~8 за 15м; text-цепочка чаще обходит OR.
- **Данные**: тот же источник — `llm_fallbacks[15m]` from_provider=openrouter,reason=openrouter_403 = 8.14. Параллельно wormsoft guard_quarantine=16.27 и chain_escalation=1.02 (фоновая норма Wormsoft, отдельного алерта нет). throttle/rate_limit за 15м = 0.
- **Вероятная причина**: не самостоятельный инцидент — это text-плечо той же vision-задачи, спотыкающейся о 403 карантинной free-модели. Диагноз и действие — см. выше (единый корень).
- **Действие**: то же, что и Vision-burst — один фикс закрывает оба.

## Отгремело за 24ч (2)
- FrontierOpenRouterModelQuarantineBurst (worker/admin) — карантин free-модели; тот же корень, что горит сейчас, ждать повторов вместе с burst-парой.
- FrontierRedisStreamOldestPendingTooOld (admin) — ушёл сам: сейчас pending/lag/oldest_pending по всем группам (enrichment/vision/crawl/reindex) = 0. Повтор маловероятен без нового poison/throughput-события.

## Чисто
- Пайплайн: `targets_down=0`, все 4 группы стримов lag=0 / pending=0 / oldest_pending=0.
- Контент свежий: last_post_age по workspace — disruption 32м, design 51м, ai_products_media 52м, ai_research/ai_trends 135м. Всё в порогах.
- Кредиты/токены: OpenRouter $19.98; GigaChat пакеты целы (GigaChat 255M, Pro 41M, embeddings 30M).
- Хост: диск / 31% (64G/216G), load 0.70, scheduler_running=1. searxng/graph_duplicate_clusters чисто.
