## TL;DR
✅ Сейчас ничего не горит (0 crit / 0 warn / 0 info). За 24ч дважды мигали OpenRouter fallback-bursts (text+vision) — уже отгремели, спилловер ушёл сам. Действий не требуется.

## Горит сейчас (0)
Пусто. `now_firing` = [], alertmanager active = [], targets_down = нет.

## Отгремело за 24ч (2)
- **FrontierOpenRouterVisionFallbackBurst** (worker · warning) — vision-запросы к `openrouter/free` ловили `openrouter_403` → фолбэк на polza `qwen3-vl-30b`. За 24ч ~22 события, **за 6ч — 0**. Ушло само (free-tier 403 транзиентный), квота OpenRouter не при чём. Повтор возможен при следующем упоре в лимиты free-tier; критичным не станет, пока polza держит спилловер.
- **FrontierOpenRouterTextFallbackBurst** (worker · warning) — `chain_escalation` по text-задачам (relevance/concepts/valence) с wormsoft → polza/GigaChat. Штатная деградация цепочки, сейчас тихо (fallbacks/throttle/rate-limit за 15м = 0). ↻ известный паттерн: [[ops_enrichment_throughput]] / [[project_wormsoft_credit_plan]] — норма при выгорании быстрого провайдера, не сбой.

## Чисто
- **Пайплайн Redis**: lag/pending/oldest_pending = 0 по всем группам (enrichment/vision/crawl/reindex).
- **Свежесть контента**: disruption 24м, design 1.3ч, ai_trends 1.8ч, ai_research 3.9ч — все ниже tier-порогов. (Аномалия: `ai_products_media` last_post_age отрицательный ≈ −2.4д — артефакт метрики/будущий таймстамп, не алертит; глянуть при случае.)
- **LLM-бюджеты**: GigaChat пакеты жирные (GigaChat 255M, Ultra 50M, Pro 41M, Max 26M, emb 28M); OpenRouter баланс $19.98; моделей в карантине 0.
- **Инфра**: все 18 контейнеров Up (core-сервисы Up 33ч, стабильны), диск / 32% (143G free), load 0.91, scheduler_running=1, SearXNG errors 0, graph duplicate clusters 0.
