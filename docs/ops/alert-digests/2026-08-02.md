## TL;DR
Горит 1 warning (0 crit / 0 info): `FrontierOpenRouterModelQuarantineBurst` — на **admin** застряло 14 бесплатных OpenRouter-моделей в карантине ~14ч. Реального ущерба нет: фолбэков/спилловера 0, кредит $19.98, воркерский free-пул здоров, пайплайн чист. **Действие сейчас не требуется** — наблюдать, при затяжке форснуть refresh каталога на admin.

## Горит сейчас (1)
### FrontierOpenRouterModelQuarantineBurst · warning · admin
- **Что**: admin видит 14 карантинных OpenRouter-моделей >10 мин; сжимается запас free-пула, растёт риск спилловера.
- **Данные**:
  - 14 моделей `service=admin` с `quarantine=1` (nvidia nemotron ×7, google gemma-4 ×2, gpt-oss-20b, cohere north-mini, poolside laguna ×2, ling-3.0-flash). Держатся непрерывно ≥6ч (`min_over_time[6h]=1`), activeAt 2026-08-01T17:20Z (~14ч).
  - `min_over_time(sum quarantine admin [24h]) = 0` → за сутки был момент с 0 в карантине, т.е. пул выел за день, стейт не «вечный».
  - **rpd_used=0 у всех карантинных admin-моделей** → это не выжигание дневного лимита, а залипший guard-стейт от прошлых ошибок upstream.
  - **Воркерский free-пул здоров**: те же модели `service=worker` имеют `quarantine=0` и активно крутятся (rpd 84–112) → сами модели не мертвы, залипла именно per-instance стража admin.
  - Спилловера нет: `increase(fallbacks[24h])` по причине `guard_quarantine` = 0, `openrouter_403` = 0; фолбэки/throttle/rate-limit за 15м пусты. Кредит $19.98.
- **Вероятная причина**: churn бесплатного пула — guard admin пометил модели по ошибкам free-tier (429/403/5xx) и не чистит их, т.к. admin эти модели почти не дёргает (rpd=0), а TTL/refresh стейт не сбросил. Классический «резерв тает, но потребитель на них не ходит». Отдельного слуга в памяти под этот паттерн пока нет (родня: `ref_openrouter_credit_metric`).
- **Действие**: наблюдать — user-facing деградации нет. Если провисит >24ч ИЛИ пойдут фолбэки с `reason="guard_quarantine"` на admin — форснуть обновление каталога/ключа OpenRouter на admin (рефреш-cron `*/5`, при необходимости `docker compose restart admin`).

## Отгремело за 24ч (0)
- Кроме самого QuarantineBurst в `fired_last_24h` ничего нет.

## Чисто
- `targets_down` пусто; все 18 контейнеров Up (redis/postgres/qdrant/neo4j/paddleocr/searxng healthy).
- Redis-стримы: lag=0, pending=0, oldest_pending=0 по всем группам (enrichment/vision/crawl/reindex).
- Свежесть: disruption 24м, ai_trends 1.6ч, ai_research 11ч, design 14ч; ai_products_media last_post_age отрицательный (будущий timestamp/скью — не алертит). Ночная тишина ожидаема.
- LLM-бюджеты: GigaChat пакеты жирные (GigaChat 255M, Ultra 50M, Pro 41M, Max 26M, emb 28M); OpenRouter $19.98. Scheduler running=1. Диск / 32% (142G free), load 0.73. Graph duplicate clusters 0.
