## TL;DR
Горит 4 (0 crit / 3 warn / 1 info). Руками нужен только **бэкап: postgres-дамп от 03:30 не читается pg_restore** — восстановление БД сейчас невозможно. Redis-pending и AlertTriageStale — транзиторные, уходят сами. Пайплайн жив, targets_down нет.

## Горит сейчас (4)

### FrontierBackupRunFailing · warning · backup
- **Данные**: прогон 03:30 — S3-заливка прошла (6 файлов, postgres.dump 615 MB), но верификация упала: `pg_restore: error: did not find magic string in file header`. 18 строк `FAILED: s3 upload` в логе — исторические. Итог: `FINISHED WITH ERRORS`.
- **Причина**: дамп залит, но битый/не в custom-формате — pg_restore не видит заголовок. Файл есть, восстановить нельзя (самый тихий отказ).
- **Действие**: проверить формат: `docker compose exec -T postgres pg_dump -Fc -U frontier frontier > /tmp/t.dump && pg_restore --list /tmp/t.dump | head`. Если ок — чинить backup-скрипт, затем `restore-stack.sh drill`. До фикса валидного бэкапа PG за сегодня нет.

### FrontierRedisStreamOldestPendingTooOld · warning · enrichment
- **Данные**: oldest ~46 мин, lag 452, pending дренится 17→11. Воркер активно обогащает (wormsoft, 200 OK, `Enriched score=0.9x`). Vision/crawl/reindex чисты.
- **Причина**: ↻ известный `ops_enrichment_throughput` — не poison (PEL движется), хвост throughput при 1 консьюмере после отгремевшего LLM-burst. Wormsoft сейчас здоров.
- **Действие**: наблюдать, уходит само. Если oldest >1.5–2ч и lag не падает — `INDEXING_MAX_CONCURRENCY` 4→6 (env + recreate worker).

### FrontierAlertTriageStale · warning · monitoring
- **Данные**: дайджест не выходил >26ч (активен ~9ч). Причина в аннотации: `start` без `exit=` = процесс убит (0xC000013A).
- **Причина**: убитый/пропущенный прогон задачи на рабочей машине. ↻ `project_daily_alert_triage_loop`.
- **Действие**: этот прогон дайджест отдаёт → погаснет сам. При повторе — `Get-ScheduledTaskInfo FrontierAlertTriage` + хвост `.claude/alert-triage.log`.

### FrontierTrendClustersStale · info · design
- `notify=never`/blackhole, в TG не идёт. Ожидаемо для низкопоточного design (развилка 29, решение владельца). Не трогать.

## Отгремело за 24ч (6)
- **FrontierGigaChatBalanceRefreshStale** (crit) — балансы сейчас свежие (GigaChat 255M и т.д.). Ушло.
- **OpenRouter ModelQuarantine / Text / VisionFallback Burst** (warn) — в карантине 1 модель, баланс $19.98. Ушло, следить.
- **WormsoftLocalThrottleBurst** (warn) — wormsoft сейчас напрямую (200 OK). Ушло.
- **FrontierNoNewPosts** (auto_hmi) — last_post ~2.25ч, в пределах порога. Ушло.

## Чисто
targets_down пусто; 18/18 контейнеров Up; last_post_age в tier-порогах; диск / 35%; throttle/rate-limit 15м = 0; graph_duplicate_clusters = 0.
