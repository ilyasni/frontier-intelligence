## TL;DR
Горит 2 (0 crit / 1 warn / 1 info). Реального инцидента нет: warning — сам факт, что вчерашняя петля /alert-triage не отдала дайджест (этот прогон её гасит). Пайплайн, бэкапы, LLM-бюджеты — зелёные. Действие сейчас не требуется, кроме проверки, почему упала вчерашняя задача.

## Горит сейчас (2)

### FrontierAlertTriageStale · warning · monitoring
- **Что**: свежий дайджест в `docs/ops/alert-digests/` не появлялся >26ч → петля /alert-triage не отработала.
- **Данные**: `last_digest_timestamp` = 36.3ч назад (порог 26ч). Все остальные метрики чистые: `targets_down`=[], lag/pending/oldest по всем 4 группам=0, scheduler=1.
- **Вероятная причина**: ↻ известный паттерн: `project_daily_alert_triage_loop.md`. Вчерашний прогон убит до записи дайджеста (0xC000013A — консольный сигнал при закрытии сессии), метка времени не обновилась.
- **Действие**: этот прогон пишет свежий дайджест → алерт погаснет в ближайшую оценку. Если повторится — `Get-ScheduledTaskInfo FrontierAlertTriage` на рабочей машине + хвост `.claude/alert-triage.log` на маркер `start` без парного `exit=`.

### FrontierTrendClustersStale · info · design
- **Что**: стабильные тренды в `design` не появлялись >7д (сейчас 15d 18h).
- **Данные**: `notify=never`, receiver=blackhole — в Telegram не уходит. Emerging-слой жив, кандидаты просто не проходят структурные ворота (≥2 кластера, ≥3 док., ≥2 источника). `last_post_age[design]`=1.7ч — контент свежий, встал именно синтез стабильного слоя.
- **Вероятная причина**: ожидаемо для низкопоточного воркспейса. Развилка 29 в `docs/AUDIT-2026-08-04.md`, ждёт решения владельца.
- **Действие**: наблюдать. Правило существует ради видимости на дашборде, побудки не требует.

## Отгремело за 24ч (7)
- FrontierBackupStale (critical) + FrontierBackupRunFailing (warning) — бэкап устаревал, свежий отработал: возраст сейчас 15.2ч (<24ч). Ушло само, повтора не ждём, если ночной прогон стабилен.
- FrontierRedisStreamOldestPendingTooOld — PEL сейчас пуст (oldest_pending=0 по всем группам). Ушло.
- FrontierClusteringCoverageCritical (auto_hmi) — auto_hmi низкопоточный (9 из 10 источников выключены), покрытие плавает. Дубликатов кластеров сейчас 0.
- FrontierOpenRouterTextFallbackBurst + FrontierOpenRouterVisionFallbackBurst — всплеск фолбэков утих: `llm_fallbacks_15m`=[], баланс OpenRouter $19.98.
- FrontierWormsoftLocalThrottleBurst — троттлинг утих: `llm_throttle_15m`=[], `rate_limit_events_15m`=[].

## Чисто
- `targets_down`=[] — все контейнеры Up (18/18), health-checked healthy.
- Redis Streams: lag=0, pending=0, oldest_pending=0 по enrichment/vision/crawl/reindex.
- last_post_age по всем воркспейсам свежий (max — design 1.7ч, ai_research 1.6ч; в пределах tier-порогов).
- LLM-бюджеты: OpenRouter $19.98; GigaChat пакеты жирные (GigaChat 255M, Ultra 50M, Pro 41M). 14 моделей OpenRouter в карантине — фон, алерта нет.
- Диск `/` 37% (76/216G), load 0.79. graph_duplicate_clusters=0 по всем воркспейсам.
