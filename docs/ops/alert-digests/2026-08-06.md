## TL;DR
Горит 1 (0 crit / 0 warn / 1 info) — `FrontierTrendClustersStale` по design, доставка выключена по построению. Действий не требуется. За 24ч отгремело 8, и все восемь объяснены: два бэкапных — хвост ложной тревоги 05.08 от сломанной ПРОВЕРКИ дампа (сама проверка починена в тот же день, ночной прогон 06.08 прошёл `OK`), триажный — вчерашний пропуск запуска (`WakeToRun` включён сегодня), остальные утихли сами. Контейнеры «Up 1–2 часа» — это мои сегодняшние пересборки, не падения.

## Горит сейчас (1)

### FrontierTrendClustersStale · info · design
- **Что**: стабильные тренды в design не появлялись 15д 21ч (порог 7д). `notify=never`, receiver=blackhole — в Telegram не уходит.
- **Данные**: `last_post_age[design]` = 4.9ч, то есть контент свежий; `clustering_coverage_ratio[design]` = 0.945 при 858 подходящих постах. Встал именно синтез стабильного слоя, а не приток.
- **Вероятная причина**: **диагноз уточнён сегодня замером, и он НЕ тот, что записан в описании алерта.** Описание говорит «ожидаемо для низкопоточных воркспейсов, ждёт решения владельца». Замер: из 830 кандидатов design за трое суток мимо гейта промоушена **ровно на одном** условии промахивается **1**, а 827 — на трёх и более. То есть пороги ни при чём: 88% кандидатов состоят из одного документа, одного кластера и одного источника, а `source_diversity` при одном источнике равен нулю **по определению**. Отличие disruption — покрытие: 101 публиковавший источник против 8 у design.
- **Действие**: наблюдать, побудки не требует. Работа переходит в раскат источников (пункт 48 реестра) — он стал предусловием пункта 29. Описание алерта стоит переписать под новый диагноз, чтобы дежурный не искал ответ в порогах.

## Отгремело за 24ч (8)

- **FrontierBackupStale** (critical) + **FrontierBackupRunFailing** (warning) — хвост ложной тревоги. Прогон 05.08 отрапортовал `FAILED: postgres dump verify` на **полностью исправном** дампе: проверка подавала архив через `docker exec -i` на stdin, а этот поток не seekable, тогда как `pg_restore` для custom-формата обязан перематывать по смещениям. Проверка починена 05.08 (`docker cp` вместо stdin), прогон **06.08 03:30 прошёл `OK`**, возраст успешного бэкапа сейчас **18.3ч**. Алерты горели до 03:30, потому что после пропуска 05.08 возраст перевалил за 36ч. Повтора по этой причине не жду. Отдельно и честно: в `cron.log` 01.08 и 02.08 тоже `start` без `OK` — те два предшествуют появлению проверки и приходятся на окно Redis-OOM, причина у них другая и не разобрана.
- **FrontierAlertTriageStale** (warning) — вчерашний прогон в 09:15 **не стартовал вовсе** (в логе нет строки `start`), дайджест дал поздний запуск по `StartWhenAvailable`, разрыв вышел 36ч при пороге 26ч. Сегодня включён `WakeToRun=True` — против режима «машина выключена» это прямое лечение. Остаток (перевод задачи на `LogonType S4U`) требует консоли администратора.
- **FrontierClusteringCoverageCritical** (auto_hmi, warning) — покрытие падало до **0.071** при **14** подходящих постах; сейчас 0.824 при 17. Причина измерена сегодня и раньше не называлась: у auto_hmi **24 из 59 постов** без `published_at`, а выборка кластеризации требует `published_at IS NOT NULL`. Это два web-источника из четырёх активных — 95% постов web-коннектора приходят без даты (пункт 75 реестра). На таких числах отношение скачет от единичного поста, поэтому абсолютный порог тут шумит.
- **FrontierOpenRouterTextFallbackBurst** + **FrontierOpenRouterVisionFallbackBurst** (warning) — всплеск фолбэков утих: `llm_fallbacks_15m` пуст, баланс OpenRouter $19.98, в карантине 14 моделей (штатный суточный сброс бесплатного тарифа, побудки не вызывает).
- **FrontierWormsoftLocalThrottleBurst** (warning) — троттлинг утих: `llm_throttle_15m` и `rate_limit_events_15m` пусты.
- **FrontierRedisStreamOldestPendingTooOld** (warning) — PEL сейчас пуст: lag, pending и oldest_pending равны нулю по всем четырём группам.

## Чисто
- `targets_down` = [] — **9 таргетов** (сегодня добавился девятый, mcp-gateway), все up; 18/18 контейнеров, health-checked healthy.
- Redis Streams: lag = 0, pending = 0, oldest_pending = 0 по enrichment/vision/crawl/reindex.
- `last_post_age` по воркспейсам в пределах tier-порогов: disruption 0.4ч, ai_research 0.9ч, ai_products_media и ai_trends 1.4ч, auto_hmi 1.9ч, design 4.9ч.
- Бюджеты LLM: OpenRouter $19.98; GigaChat 255.5M, Ultra 50M, Pro 41.7M, Max 26.4M, эмбеддинги 21.7M.
- Диск `/` 37% (76 из 216 ГБ), load 0.52. Квота S3 — **0.665** (метрика починена сегодня: до этого экспортировалась зажатой в INT32_MAX, из-за чего оба квотных алерта были недостижимы при любом наполнении).
- `graph_duplicate_clusters` пуст, `searxng_errors_15m` пуст, планировщик admin работает.
