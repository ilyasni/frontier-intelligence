## TL;DR
Горит 3 (0 crit / 2 warn / 1 info) — все три остаточные, а не новые. Оба warning'а по сути вылечены и ждут, пока сползут окна. Действие сейчас не требуется. За сутки отгремело 11 алертов, включая критические по disruption, Redis и S3 — все закрыты, подтверждено свежими данными.

## Горит сейчас (3)

### FrontierAdminJobFailingDaily · warning · admin / run_signal_analysis
- **Что**: 3 отказа `run_signal_analysis` за двое суток.
- **Данные**: отказы датированы — 15.08 16:20, 16.08 00:20, 16.08 08:20 UTC, шаг 8ч. После них **6 прогонов `ok` за 12ч, ни одного `failed`**. В `cluster_runs` **0 записей `running`** (осиротевший `running` — подпись SIGKILL, по которой ловился OOM). Ночной прогон disruption 00:20:03→00:21:22 = **79 секунд** против 20 минут до фикса.
- **Причина**: ↻ известный паттерн: `project_signal_analysis_oom_fix` — хвост серии из 28 memcg-OOM (07–16.08). Окно правила 2d1h, поэтому оно светится отказами, случившимися **до** раската блочного матмула 16.08.
- **Действие**: наблюдать, уйдёт само около **01:20 UTC 18.08**. Если останется после — это уже новый отказ, тогда `docker logs frontier-intelligence-admin-1 | grep "failed for workspace"`.

### FrontierAlertTriageStale · warning · monitoring
- **Что**: дайджест не появлялся дольше 26ч.
- **Данные**: последний — 07.08 06:17 UTC, простой **10 суток ровно**.
- **Причина**: ↻ известный паттерн: `project_daily_alert_triage_loop` — не стек, а авторизация: задача запускалась ежедневно, но умирала за 3 секунды на `Not logged in`. Разбор переехал на локальное расписание 09:30, **этот прогон — первый успешный за 11 суток**.
- **Действие**: ничего, доставка обновляет метрику — погаснет за ~5 минут.

### FrontierTrendClustersStale · info · design *(notify=never)*
- **Что**: стабильных трендов в design нет больше недели.
- **Данные**: последний **26.3 суток назад**, но приток живой: `last_post_age` = 1.4ч, полная кластеризация 05:07→05:08 успехом, дубликатов 0.
- **Причина**: не поломка, а структура — ↻ `project_clustering_coverage`: 827 из 830 кандидатов промахиваются мимо гейта по трём условиям сразу, 88–94% — один документ и один источник, `source_diversity` при одном источнике равен нулю по определению. Узкое место — 8 публикующих источников против 101 у disruption.
- **Действие**: пороги не трогать (промотирует одиночек, а проверить нечем — `card_feedback` пуст). Ждёт пункт 48 реестра.

## Отгремело за 24ч (11)
- **AnalysisStaleCritical + AnalysisStale** (disruption) — ушли с OOM-фиксом, свежесть теперь 1.5ч. Повтора не ждать.
- **NoNewPostsCritical + NoNewPosts** (auto_hmi), **NoNewPosts** (ai_research) — ночная тишина контента, не сбой: `last_post_age` 2.6ч. Повторится следующей ночью.
- **RedisStreamLagCritical / LagHigh / OldestPendingTooOld** — сейчас lag 296, pending 4, старейший 18 мин. ⚠️ Единственное, за чем присмотреть: lag за 2ч рос 0→55→296. Похоже на утренний наплыв; если к полудню не сойдёт к нулю — `XINFO GROUPS stream:posts:parsed` на застрявший PEL.
- **S3QuotaCritical** — закрыт: **7.89 из 16.1 ГБ (49%)**, замер свежий. Синхронная уборка от 17.08 сработала.
- **ManualSignalJobFailedRecently** — планировщик = 1, повторов нет.
- **WormsoftLocalThrottleBurst** — фолбэки и throttle за 15 мин пусты.

## Чисто
`targets_down` пуст. Все 18 контейнеров подняты, рестартов нет. Диск 33%. OpenRouter $19.98, карантин 0. GigaChat 255M базовой / 50M Ultra. Дубликаты кластеров 0 везде.
