## TL;DR
Горит 1 info (`FrontierTrendClustersStale`, design, notify=never → blackhole) — действий не требует. Critical и warning — ноль. За последний час полностью тихо: throttle Wormsoft = 0, фолбэков = 0, PEL пуст, targets_down пуст. Вмешательство не нужно. Это второй прогон за сутки (первый — 08:43 UTC), картина с тех пор не изменилась.

## Горит сейчас (1)

### FrontierTrendClustersStale · info · design
- **Что**: стабильные тренды не появлялись в design больше недели.
- **Данные**: activeAt 04.08 21:03 UTC, value 2 364 270 с = 27 д 8 ч 45 м. Приток живой: `last_post_age` design = 12 821 с (3 ч 34 м) при пороге low-vol 24 ч; `graph_duplicate_clusters` = 0 по всем шести воркспейсам. Маршрут — receiver `blackhole`, метка `notify=never`.
- **Вероятная причина**: ↻ известный паттерн: `project_clustering_coverage` (+ замер 07.08, `docs/TODO-UNFINISHED.md` п. 29). Дело не в порогах: 827 из 830 кандидатов промахиваются мимо гейта на трёх и более условиях, 88–94 % состоят из одного документа/кластера/источника, `source_diversity` при одном источнике равен нулю по определению. Свежие данные гипотезу подтверждают: приток и дедуп целы, значит упирается в структуру кандидатов, а не в поломку.
- **Действие**: не трогать. Правило намеренно живёт только на дашборде. Закрывается п. 48 (раскат источников design — 8 публикующих против 101 у disruption); понижение `trend_cluster_stable_threshold` промотирует одиночек, и проверить это будет нечем — `card_feedback` пуст.

## Отгремело за 24ч (4)

- **FrontierWormsoftLocalThrottleBurst** (worker, `reason=wormsoft_credit_soft_cap`) — 535 срабатываний кредит-гарда за сутки; за последний час и за 15 мин — **0**, фолбэков wormsoft→polza за час тоже 0. ↻ `project_wormsoft_credit_plan`: тариф 3M/4ч выгорает, гард уводит на медленный polza. Штатный спилловер — повтора жду в следующем 4-часовом окне, это не поломка.
- **FrontierRedisStreamOldestPendingTooOld** (admin, `stream:posts:parsed`/`enrichment_workers`) — max за 24 ч = 3363 с (56 мин) при пороге 1800; сейчас 0, lag 0, pending 0 по всем четырём группам. Следствие пункта выше (энричмент шёл через медленный polza), а НЕ poison PEL: ↻ `ops_enrichment_throughput`, не `project_enrichment_poison_pending_fix`. Повторится вместе со следующим выгоранием тарифа.
- **FrontierAdminJobFailingDaily** (admin, `run_signal_analysis`) — ушёл окном: за 49 ч теперь 1 падение при пороге ≥2, за сутки 18 прогонов и все `outcome=ok`; за 7 суток — 13 failed / 101 ok, то есть отказы кучно легли до 16.08. ↻ `project_signal_analysis_oom_fix`: фикс блочного матмула от 16.08 подтверждён свежими данными, повтора не жду.
- **FrontierAlertTriageRunFailing** (monitoring) — отгремел ровно один шаг: `last_exit_code` держался 1 до 17.08 ~11:00 UTC (сбойный прогон, длительность 420 с), с прогона 17.08 11:02 UTC — `exit_code=0`, `duration=229 с`. Длительность в минутах, а не в секундах → это НЕ подпись разлогина CLI. ↻ `project_daily_alert_triage_loop`: наблюдаемость, поставленная 17.08, отработала как задумано — отказ был виден и закрылся сам следующим прогоном.

Выпали из окна 24 ч и потому не в списке: `FrontierOpenRouterVisionFallbackBurst` / `FrontierOpenRouterTextFallbackBurst` и `FrontierAlertTriageStale` — разобраны в дайджесте 08:43 UTC, повторов с тех пор нет.

## Чисто
- `targets_down` пуст, все 18 контейнеров Up; admin поднят 18 ч назад, RestartCount не растёт.
- `last_post_age` по воркспейсам в пределах tier-порогов: disruption 34 м, ai_products_media 1 ч 16 м, ai_trends 1 ч 35 м, design 3 ч 34 м, ai_research и auto_hmi 5 ч 6 м (low-vol, порог 24 ч).
- Redis: lag 0 и pending 0 по всем четырём группам (enrichment / vision / crawl4ai / reindex), oldest pending 0.
- `admin_scheduler_running` = 1. Диск / — 34 % (69 G из 216 G), loadavg 0.75.
- LLM: GigaChat 255 M базовых + 50 M Ultra + 41,7 M Pro токенов, эмбеддинги 11,5 M; OpenRouter баланс 19,98 $. В карантине один модель-ид — `google/lyria-3-pro-preview` (генерация музыки), в наших цепочках не используется, влияния нет.
- SearXNG-ошибок за 15 мин — 0; дублей кластеров — 0 по всем воркспейсам.
