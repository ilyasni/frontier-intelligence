# Объединение зависимостей всех сервисных образов (worker/ingest/mcp/admin) + тест-инструменты.
# Используется в CI (.github/workflows/ci.yml) для прогона unit-тестов одним job'ом.
# Пины синхронизированы с */Dockerfile — при изменении Dockerfile обнови и здесь.

# --- shared / worker / admin ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
redis==5.0.4
prometheus-client==0.20.0
pydantic==2.7.1
pydantic-settings==2.3.1
pyyaml==6.0.1
httpx==0.27.0
openai==1.54.4
boto3==1.34.131
qdrant-client==1.12.0
neo4j==5.23.1
ruptures==1.1.9
fastembed==0.8.0
onnxruntime==1.24.4

# --- mcp / admin (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- ingest ---
telethon==1.36.0
PySocks==1.7.1
socksio==1.0.0
apscheduler==3.10.4
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.2.2
croniter>=1.3.8

# --- test tooling ---
pytest==8.3.4
pytest-asyncio==0.24.0
